# 2026-08-19 — Run 47 (RE): source-diff review — upstream #51 + #58 (unreleased main)

## Why this run

All three DIRECTION (supervisor run #6) priorities were already complete by the
end of run 46 (#1 RE-45→test36, #2 upstream#52→RE-47/48, #3 install-cli→test37).
With nothing left in the priority list, I checked the source clone
`~/monet/monet` for drift and found it **7 commits behind `origin/main`**. Two of
those are substantive code changes — `81976e5` (#51, 2026-08-18) and `683d261`
(#58, 2026-08-19) — the rest are docs/CI. This run is a source-diff review of
those two, which is the "re-check on version bump" discipline applied one step
early (npm `@team-monet/monet` is still 1.6.3; nothing has shipped).

## What changed (readable TS, packages/core + packages/cli)

### #51 — "Make the gate's record say which rule blocked, and surface the read dimension"
- **`gate-journal.ts`** — new `blockingRuleIdsOf(rules)` helper: returns
  `{ blockingRuleIds }` only when a blocking rule matched, else `{}`. Shared so
  the two writers ("core-gate" and "gate-cli") stop drifting (monet#37).
- **`gates.ts`** — `gateStats` gains `byStageRead` (per-stage count of
  `stage_lookup` "recognized" deliveries, `rule_count > 0`, liveness-scoped).
  New index `idx_gate_events_stage_lookup ON gate_events(matched_stage_id,
  matcher, circle, ts)`, created in `migrateGateColumns` AFTER the `matcher`
  column guard (the CREATE-before-ALTER ordering bug was caught by the suite's
  schema-upgrade test).
- **`engine.ts`** — `MemoryOverview.gateStats` gains `unreadStages` (zero-read
  stages, capped at `OVERVIEW_EXCEPTION_LIMIT`=10) + `unreadStagesOmitted`.
  The hook-path journal write now uses `...blockingRuleIdsOf(result.rules)`
  instead of the inlined (and silently-skipped) version.
- **`mcp-server.ts`** — `fitOverviewEnvelope` drops `unreadStages` the same way
  as `unexplainedDenies` (count survives via `unreadStagesOmitted`).
- **`render-overview.ts`** — renders `unread/{windowDays}d [id] name` lines and
  a `no curation work queued` gate now also consults `unreadStages`.

### #58 — "Hook sends rule identity and a read instruction, not clipped rule text"
- **`gate-cli.ts`** — `formatRuleLine` (`text — reason`, 80-char clip) replaced
  by `formatGateInstruction`: identity-only payload (matched stage names,
  blocking count, an instruction to read via `stage_lookup`), capped at
  `INSTRUCTION_STAGE_CAP`=8. The `reason` field is now delivered by
  `stage_lookup` (pull), not pushed by the hook. Deny discloses blocking
  concept ids + mirror generation + "later edit is unrecoverable".
- **`install-cli.ts`** — the deny path now writes `enforced: true` (monet#37);
  the advisory `additionalContext` appends a timing sentence (advisory arrives
  beside the result, not before the call).
- **`engine.ts` / `gates.ts` / `mcp-server.ts`** — the `reason` field's
  describe strings + error wording change from "shown when the gate fires" to
  "delivered by stage_lookup, not by the gate payload".
- **`index.ts`** — re-exports `CIRCLE_NAME_MAX_CHARS` (so the gate payload
  can't restate the stage_lookup circle bound and drift from it) and
  `blockingRuleIdsOf`.

### #37 — conformance attribution (folded into #51)
- **`conformance.ts`** — `computeConformance` now folds the two sibling mouths
  (wrapper + `monet gate`) into one fire via `foldChainsToOneFire` (representative
  = line naming the most rules) and reads `enforced` across the chain via
  `buildChainEnforced`. `verdictRuleIds` absent ⇒ single-rule fire (was "applies
  to all"); empty ⇒ "unavailable" (multi-rule fire with no recorded blocking ids
  → no rule credited, reason string appends the caveat). `annotatedScoped` lets
  a prior annotation written under the OLD contract be re-annotated once.

## Impact on the registry

- **Upstream #49/#50 (hook clips rule directive to 80-char) → FIXED** in `main`
  (identity payload removes the clip; `reason` deliberately excluded so the #50
  "does reason motivate the read" measurement is not contaminated).
- **Upstream #37 (conformance over-credits hook-path denies + double annotation)
  → FIXED** in `main` (`foldChainsToOneFire` + `buildChainEnforced` +
  `blockingRuleIdsOf` + `enforced: true`).
- **Upstream #28 (read dimension) → SHIPPED** with a new MCP surface:
  `memory_overview.gateStats.unreadStages` / `unreadStagesOmitted`.
- **No RE issue status flipped** — npm is still 1.6.3; the fixes are unreleased
  `main`. Re-verify the whole XFAIL set + these three upstream items at the next
  release (candidate 1.7.0).
- **RE-26** (gate_events retention), **RE-27** (advisory judgment half),
  **RE-28** (action_context raw storage), **RE-48** (ack title/slug) — NOT
  touched by these commits.

## Halt-invalidation flag (for the supervisor)

DIRECTION run #6 halt-listed gate/dashboard RE and the core-internal upstream
batch (#27-33/36/37/49/50) on the rationale "no MCP/CLI surface". That rationale
is now **partially false**:

- `memory_overview.gateStats.unreadStages` is a NEW, MCP-observable surface (the
  read dimension, #28).
- Three of the halt-listed items (#37, #49, #50) are now **fixed in source**.

Recommendation: when the next release lands, the supervisor should (a) un-halt a
scoped re-verification of `gates`/`conformance`/`gate-cli` (the payload + read
dimension are now observable and E2E-verifiable), and (b) queue a full XFAIL
re-run. No action this run — recording only.

## Metric changes

- Modules documented: 24 (unchanged — no new module documented, gate/conformance
  are already in `gates.md`/`conformance` coverage; this is a diff review).
- Issues: 48 (unchanged — the fixed upstream items #37/#49/#50 were never
  registered as RE-NN, and the fixes are unreleased).
- New params/constants identified: +9 (see METRICS "Identified parameters").

## Next steps

- Re-verify the 14 XFAILs + upstream #28/#37/#49/#50 at the next
  `@team-monet/monet` release (npm 1.6.3 → next).
- If the read dimension ships, `memory_overview.gateStats.unreadStages` becomes a
  candidate for a scoped E2E scenario (currently no gateStats-specific test
  beyond test29's retention angle).
- Await supervisor direction on whether to lift the gate halt for a scoped
  re-analysis post-release.

## Meta

- Supervisor: run #6 (2026-08-18). Worker: RE.
- No suite run (unreleased source; the review is source-level only).
- Source clone `~/monet/monet` fast-forwarded 7 commits to `origin/main`
  (read-only analysis target; `.cov-raw/` untracked dir left alone).

---

# 2026-08-19 — Run 48 (RE): upstream #59 + #13 triage → RE-49 + RE-50

## Why this run

DIRECTION (supervisor run #7) has three priorities: #1 is the E2E worker's
(gate-cli coverage), #2 and #3 are RE triage tasks — both upstream
`team-monet/monet` issues. This run verifies both against the current readable
source, registers them, and routes them (XFAIL for #59 → E2E; L2 for #13).

## Priority #2 — upstream #59 → RE-49 (stage_lookup clips body/reason, no recovery path)

Verified against `packages/core/src/mcp-server.ts` (current `main`):

- `clip()` (146-150) returns `{text, clipped}` and writes an inline
  `…[truncated N chars]` marker — matches the issue's quote byte-for-byte.
- `stage_lookup` handler (1595-1625) computes `bodyClip`/`reasonClip` but uses
  ONLY `.text`; the `.clipped` flag is **discarded**. No `bodyTruncated`/
  `reasonTruncated` field is emitted. `conceptId` IS returned (1598), so the
  recovery path exists (`memory_fetch(conceptId)`) but is undisclosed.
- `memory_fetch` already solves this: emits `...(body.clipped ? {bodyTruncated:true} : {})`
  (1519) and its tool description says "`bodyTruncated` means recover from
  observations" (1407). `stage_lookup` has the same problem and neither the flag
  nor the instruction.
- `stage_lookup` tool description (1566) promises "omission recovery fields" —
  those are `rulesOmitted`/`omittedRules` (omitted RULES, the 3-tier ladder),
  NOT clipped body/reason. The gap is real and un-addressed.
- Caps confirmed at `gates.ts:2018-2021` (`STAGE_LOOKUP_RULES_CAP`=200,
  `BODY_CAP`=6000, `REASON_CAP`=1200, `OUTLINE_CAP`=500) — already in METRICS.

**E2E-verifiable: yes** (MCP surface, `stage_lookup` is one of the 23 tools, not
halt-listed). Desired contract: the response discloses
`bodyTruncated:true`/`reasonTruncated:true` when clipped (mirroring `memory_fetch`),
so a caller can `memory_fetch(conceptId)` to recover. Currently absent → XFAIL.
Registered as **RE-49 (S3, open)**. Flagged as E2E follow-up **test39**.

## Priority #3 — upstream #13 → RE-50 (startup can't say why it died)

Verified against `packages/core/src/mcp-server.ts` + `packages/core/scripts/mcp-cli.ts`:

- `createMonetCoreMcpServer` is now at mcp-server.ts:3454 (the issue cited
  3358-3414 — pre-monorepo line numbers; the investigation is unchanged).
- `await core.ensureEmbedderPin()` (3466) — model load/warm/download — runs
  BEFORE `new McpServer` (3467): the server object does not exist until the pin
  is satisfied.
- `new StdioServerTransport()` (3490) + `await server.connect(transport)` (3491)
  — the protocol channel opens **LAST**. Every throw before 3491 leaves no MCP
  channel; the host sees only "Connection closed" (-32000).
- The factory's try/catch (3489-3508) wraps only connect + scheduler attach;
  `ensureEmbedderPin()` (3466) and the entry points' store-open + model-load are
  OUTSIDE it.
- Entry point `packages/core/scripts/mcp-cli.ts:38-51` confirms the fallible
  region is wider than the factory: `new MonetCore(dbPath, { embedder: await
  chooseStartupEmbedder(dbPath) })` opens the store AND loads the model before
  `createMonetCoreMcpServer(core)` (52). `main().catch()` (61-66) writes the
  cause to stderr + exit 1 — invisible on a stdio transport.

**E2E-verifiability judgment: structural/design.** The fail-closed order is
deliberate (never substitute another embedder — see `instantiateEmbedderForPin`),
so fixing the opaque exit requires a design decision (degraded serving mode vs
out-of-band diagnosis vs narrowing causes), not a pure reorder. No clean
behavioral XFAIL without the design answer. Registered as **RE-50 (S2, source)**
→ L2 routing (design-decision queue).

## Metric changes

- Issues: 48 → 50 (RE-49, RE-50). Modules documented: 24 (unchanged — mcp-server.ts
  already documented). New params/constants: 0 (the four STAGE_LOOKUP caps were
  already recorded).

## Next steps

- E2E worker: convert RE-49 to XFAIL test39 (recipe: create a stage whose rule
  body > 6000 chars via the gate/declare path → `stage_lookup` → assert
  `bodyTruncated`/`reasonTruncated` disclosed).
- Supervisor/John: RE-50 joins the L2 design-decision queue (upstream #13 options
  1/2/3 — degraded mode / out-of-band diagnosis / narrow the causes).
- Re-verify both at the next `@team-monet/monet` release.

## Meta

- Supervisor: run #7 (2026-08-19). Worker: RE.
- Source clone `~/monet/monet` at current `main` (post run-47 fast-forward).
- No suite run (verification-only scope; XFAIL writing is the E2E worker's step).
# 2026-08-19 — Run 48 (E2E / coverage): `monet gate` CLI scenario (issue #15)

## Priority taken

DIRECTION (supervisor run #7) priority #1 was `[E2E] gate-cli.ts coverage
scenario` (monet-e2e#15): gate-cli.ts sat at 6.9% (74/1078), the last big
non-deprecated CLI gap. Gate needed a materialized mirror to evaluate against —
the run builds one by hand and drives `monet gate` through the process boundary.

## What was added

- **`tests/test39_gate_cli.py`** (56 assertions) — the offline `monet gate`
  evaluator's five-outcome exit-code contract + gate journal + fail-open policy,
  driven via the harness `run_cli` (same CLI-subcommand pattern as test35/37).
  Fixture: hand-built GATE_MIRROR_FORMAT=4 mirror (entries/stages/circleAliases/
  circles + sha256 checksum computed with the EXACT JS `JSON.stringify(x,null,2)`
  recipe via a node one-liner — Python's `json.dumps` separators differ, verified).
  Contributes to gate-cli.ts coverage and exercises 4b-C/4b-D surfaces.
- **`harness/mcp_client.py`** — `run_cli` gained an optional `stdin=` param (for
  `monet gate --stdin`). Backward compatible.
- **`coverage-gaps.md`** — regenerated (new baseline row).

## Contract pinned (gate-cli.ts five outcomes, five exit codes)

USAGE error=1 (no/ambiguous context, unprefixed context, bad `--tool`,
`--circle '*'`) · SILENCE=0 · STAGE_HIT_NO_RULES=10 · ADVISORY=20 ·
BLOCKING=30 · OVERFLOW=40 · fail-OPEN=0 (missing/malformed mirror carries the
`failing OPEN` marker on stderr, distinct from genuine silence). Journal
(`MONET_STORAGE_DIR/gate-journal.jsonl`) verified: `disposition` per verdict
(deny/advisory/silent/stage-hit-no-rules/overflow/declined:*), `claimType`
'parsed' (mirror answer, not a live store read), `ruleIds`, `gateExitCode`.

Arms: blocking/advisory/stage/no-rules/silence verdicts; circle-alias delivery
(query `oldacme` → rule bound at `acme`, single hop); model-tag scope filtering
(agent rule delivers only when `MONET_MODEL_TAG` matches or is unset — via
journal `ruleIds`); Tool:-prefix discipline (refusal, `--tool` synthesis, `--tool`
ignored warning); positional vs `--stdin` sourcing (both → usage error); overflow
via a ~4.3 MiB piped context (exit 40, RETAINED_STDIN_CAP path); tampered-checksum
→ malformed → fail open; wrong-format/missing-stageId references → malformed.

## Version-drift guard (important for this module)

The installed binary is 1.6.3 (gate payload = `text — reason`); the coverage
bundle is built from the source clone at `main`, which ships #49/#58 (identity-only
payload: `Blocked by a Monet rule — N ...` + `stage_lookup` instruction). The five
EXIT CODES and the JOURNAL record are identical across both, so test39 asserts on
those + stdout-emptiness — never verbatim payload text. It is green against BOTH
the regular installed runs and the coverage bundle (verified both ways).

## Coverage result

- **gate-cli.ts: 6.9% → 55.0%** (74/1078 → 675/1228 — total grew because `main`
  adds the #49/#58 identity-payload lines).
- **Total line coverage: 81.9% → 82.9%** (44,738/53,987; source file set 50, lines
  grew 53,505 → 53,987 from `main` drift).
- Full suite under the coverage bundle: 23/39 pass + 14 xfail + 1 xpass, **0 FAIL**
  (test39 included). Installed-suite: 24/39 + 14 xfail + 1 xpass, 0 FAIL.
- gate-cli.ts is no longer the top non-deprecated CLI gap. Remaining per the
  coverage pipeline (DIRECTION): cli/circle.ts 36.5%, core/conformance.ts 33.1%,
  core/spans.ts 39.8%, cli/db/index.ts 27.9%, cli/bootstrap.ts 22.4%.
- Deprecated source modules (source-git 3.1%, source-chunker 5.7%) and
  render-overview.ts 6.9% (RE-37 permanent gap) remain excluded by DIRECTION.

## Issue handling

- **monet-e2e#15 (gate-cli coverage)** → COMPLETE. Commented + closed on GitHub.

## Next run

- Priority #2/#3 (RE triage upstream #59 → RE-NN; upstream #13 → RE-NN) target
  the RE worker. For the next E2E/coverage run, the pipeline points at
  **cli/circle.ts (36.5%)** as the next CLI-subcommand scenario, then core
  conformance.ts (33.1%) / spans.ts (39.8%). No version bump observed (npm still
  1.6.3; `main` ahead) → the full XFAIL set (plus #37/#49/#50, #28 read dimension)
  stays queued for the next release.
# 2026-08-19 — Run 49 (E2E / coverage): `monet start` circle derivation — cli/circle.ts (issues #16/#17 closed)

## Priority taken

DIRECTION (supervisor run #7) priority #1 (gate-cli → test39) was completed in run
48. The coverage pipeline for the next E2E run pointed at **cli/circle.ts (36.5%)**
— the remote→circle resolver called by every `monet start`, and the last big
non-deprecated CLI-surface gap without a dedicated scenario.

## What was added

- **`tests/test40_circle_cli.py`** (10 assertions) — drives `deriveCircle` through the
  `monet start` stderr `Circle:` banner (cli.ts:125 prints `Storage:`/`Circle:` to
  stderr before the town server blocks). Arms:
  A. `MONET_CIRCLE` env override wins outright and is TRIMMED (`"  my-override  "` → `"my-override"`).
  B. no git / no remote → folder-hash fallback (`deriveFolderCircle`, e.g. `plain-<hash>`).
  C. genuinely-new repo + origin remote → `defaultNameFromRemote`
     (`github.com-team-monet-e2e-circle`), and NO `remote_circle_map` row is persisted
     (green field: the genuinely-new branch returns a name WITHOUT writing a map).
  D. mapped remote (seed `remote_circle_map` for the canonical key) → returns the mapped circle.
  E. Class A: no map but the folder-hash slug aliased in `circle_aliases` → follows the alias.
  F. Class B: no map/alias but memory (a `concepts` row) under the folder-hash slug → keeps
     the slug AND writes a `remote_circle_map` row (the anti-orphan guard).
  G. after the Class-B writeMap, a re-run resolves via readMap → same stable circle.
- Fixtures are real throwaway git repos (`git init` + origin remote + committed file)
  in per-arm temp dirs; every store is `-d` temp — never ~/.monet (GR-01).

## Coverage result

- **cli/circle.ts: 36.5% → 37.8%** (170→176/466) — first time this module's number
  moved; DIRECTION pipeline target cleared.
- **Total line coverage: 44,738 → 44,744 (82.9%)** — a modest +6-line gain. The
  executable surface of `deriveCircle` (override / folder / mapped / Class A / Class B)
  is small and the file is comment-heavy; the meaningful value is the branch coverage
  this proves, not a large line delta.
- Full suite under the coverage bundle: 25/40 pass + 14 xfail + 1 xpass + **0 FAIL**
  (test40 included; green on installed 1.6.3 AND the coverage bundle).
- Remaining pipeline (DIRECTION): **core/conformance.ts 33.1%, cli/db/index.ts 27.9%,
  core/spans.ts 39.8%, cli/bootstrap.ts 22.4%**.

## Pitfall recorded (coverage methodology)

A coverage-driven test that spawns the MCP server MUST let it exit CLEANLY (close
stdin → EOF → graceful shutdown) — a **SIGKILL skips the V8 coverage flush entirely**,
silently under-reporting (first version killed the process and circle.ts did NOT move;
switching to the harness `MonetClient.close()` = EOF fixed both shutdown and coverage).
test40 therefore reuses the harness client rather than raw `subprocess.kill()`.

## Issue handling

- **monet-e2e#16 (RE: triage upstream #59 → RE-49)** — triage completed by the RE
  worker in run 48 (RE-49 registered, S3/open, E2E-verifiable via `stage_lookup`). The
  `stage_lookup` clip XFAIL is NOT yet written (RE worker's note planned it as "test39"
  but that slot went to gate-cli in run 48). **Closed with a comment** recording RE-49
  registration + that the RE-49 XFAIL remains queued (see Next steps).
- **monet-e2e#17 (RE: triage upstream #13 → RE-50)** — triage done run 48 (RE-50,
  S2/source→L2 design queue). **Closed with a comment** recording RE-50 routing.

## Next run

- E2E: convert **RE-49 (stage_lookup body/reason clip, no recovery disclosure)** to an
  XFAIL test — recipe: a stage whose rule body > 6000 chars (caps at gates.ts:2018-2021,
  BODY_CAP=6000 / REASON_CAP=1200), `stage_lookup` it, assert the DESIRED contract that
  `bodyTruncated`/`reasonTruncated` are disclosed (mirroring `memory_fetch`), absent → XFAIL.
- Coverage pipeline: **core/conformance.ts (33.1%)** or cli/db/index.ts (27.9%) next —
  conformance needs a gate evaluate; db/index is a small path-resolution module (likely a
  quick win).
- Re-verify the 14 XFAILs + upstream #28/#37/#49/#50 at the next `@team-monet/monet`
  release (npm still 1.6.3; `main` ahead).

## Meta

- Supervisor: run #7 (2026-08-19). Worker: E2E.
- Suite run under the coverage bundle (`cli.coverage.js` built from `main`).
- Env healthy (disk 38%).
---
# 2026-08-19 — Run 50 (RE): RE-49 → XFAIL test41, CONFIRMED

## Why this run

DIRECTION (supervisor run #7) assigned the RE worker priorities #2 (upstream
#59 → RE-NN) and #3 (upstream #13 → RE-NN). Reading the repo state first made
clear both were **already triaged in run 48 (RE)**: RE-49 (S3/open) and RE-50
(S2/source) are registered and verified in `ISSUES.md` and `diary/2026-08-19.md`.
Re-doing them would duplicate finished work.

The one genuinely-pending item scoped to my assigned triage was RE-49's XFAIL:
run 48 (RE) deferred XFAIL-writing ("the E2E worker's step", flagged test39),
and E2E runs 48/49 both re-queued it ("the RE-49 XFAIL remains queued"). Neither
had written it. Per the "정체 시 다른 각도 시도" guardrail and the shared E2E
verification loop, this run wrote the RE-49 XFAIL and confirmed it.

## What was verified (source cross-check, `main`)

- `clip()` at mcp-server.ts:146-150 returns `{text, clipped}` and writes the
  inline `…[truncated N chars]` marker — matches run 48's quote byte-for-byte.
- `stage_lookup` handler (mcp-server.ts:1597-1625) computes `bodyClip`/
  `reasonClip` but uses only `.text` (reason at :1600, body at :1625) and
  DISCARDS the `.clipped` flag. No `bodyTruncated`/`reasonTruncated` emitted.
- Caps confirmed at gates.ts:2018-2021 (`STAGE_LOOKUP_RULES_CAP`=200,
  `BODY_CAP`=6000, `REASON_CAP`=1200, `OUTLINE_CAP`=500) — already in METRICS.
- `memory_declare` `content` is `z.string().optional()` (mcp-server.ts:1017) —
  NO max, so a rule body > 6000 chars is declarable and the wire clip will fire.

## The XFAIL: tests/test41_re49_stage_lookup_truncation.py

Deterministic recipe (isolated `-d` temp store, GR-01/GR-06):
1. `memory_declare` species=stage (fresh timestamped name).
2. `memory_declare` species=rule, advisory, content = 8100 chars (> BODY_CAP).
3. `stage_lookup` → matched=True, 1 rule delivered, `body` carries the
   `…[truncated 5079 chars]` marker (bodyLen 6021) → the clip FIRED.
4. Assert DESIRED contract: a clipped body must carry a truthy
   `bodyTruncated`/`reasonTruncated` disclosure (mirroring `memory_fetch`).
   Delivered rule keys = `{body, conceptId, origin, reason, reasonMissing,
   scope, severity, text}` — NO disclosure field → **XFAIL (exit 2)**.

Result: RESULT: XFAIL RE-49 (6 setup checks passed). Reproduces identically on
the installed 1.6.3 binary.

## Metric changes

- RE-49: `open` → `confirmed`, `e2e_test` `pending-XFAIL` → `test41`.
- Modules: 24 (unchanged). Params: 284 (unchanged — BODY/REASON caps already
  recorded). Issues: 50 (unchanged in count — a status flip, not a new issue).

## Next steps

- RE-50 (S2, source) stays in the L2 design-decision queue (upstream #13 —
  degraded mode vs out-of-band diagnosis). No E2E action — structural/design.
- Re-verify the whole XFAIL set (now including test41) + upstream #28/#37/#49/#50
  at the next `@team-monet/monet` release (npm still 1.6.3; `main` ahead).
- RE-01 is now the only remaining `open` behavioral issue (source-deprecation
  path — run 38); no further XFAIL work actionable this run.

## Meta

- Supervisor: run #7 (2026-08-19). Worker: RE.
- Source clone `~/monet/monet` at `main`; single-test run (no full suite —
  verification/scope aligned with DIRECTION "검증만").
- Env: exported MONET_CLI + MONET_NODE_PATH (node@22) for the harness; prod
  `~/.monet` untouched.

