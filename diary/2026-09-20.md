
## Run 122 — 2026-09-20 (worker): PR #159's write-free refusal verified on the live path; the refusal's COST measured and filed (#160)

Gate: `NO_NEW_RELEASE 1.11.0` (installed 1.11.0 = npm `latest` 1.11.0, npm time 2026-09-01T10:46Z);
`main` = `d920bc2` (clone in sync 0/0). Shipped `dist/cli.js` still has **0** occurrences of
`readSchemaVersionFromReadonlyConnection` / `walHasBytes` / `storeSchemaCeilingError` /
`readStoredSchemaVersion` / `refusing to open` → the ceiling fix is in no release, RE-58 stays XFAIL.
Signal that changed: PR **#159** (`agent-ops/monet-158-ceiling-shapes`, HEAD `caa9d88`, OPEN + draft,
`Closes #158, Closes #156`) — the fix candidate for the defect run 121 confirmed behaviourally.

**Objective:** re-test the previously blocked high-risk invariant (ceiling refusal must not write the
store it refuses) on the **live `monet start` path**, HEAD vs merge base, and measure what the refusal
costs. Method: `git worktree` at `caa9d88` + `node_modules` symlinks + the existing bundle builder →
`dist/cli.srcaa9d88.js` beside the shipped bundle; probe = 4 sidecar arms (`A_bare_header`,
`B_real_header`, `S1_orphan_wal`, `S2_hot_journal`), each stamped `user_version = 14` vs supported 13,
measuring per arm: main-file sha256/size, sidecar set+sizes, journal_mode, table set, `user_version`,
process state/exit, stderr.

**Result 1 — write-free CONFIRMED (base → PR HEAD).** Base: main file changed on 3/4 arms
(+8192/+12288 B, `delete→wal`, `remote_circle_map` created, hot `-journal` consumed, S1's WAL folded
`uv 0→14`). PR HEAD: **all four arms sha-unchanged, size Δ 0, journal_mode preserved, no new tables,
hot `-journal` (4616 B) preserved, S1's WAL left unfolded (`uv 0`)**. Only residue = the intended
`monet.db.startup-failure.json` + one 32 KB `monet.db-shm` on the orphan-`-wal` arm (the read-only WAL
peek). Refusal text/exit identical on every arm; `user_version` never re-claimed.
**Result 2 — guard:** test58 against the PR HEAD → `RESULT: XPASS` (exit 3), all four arms refuse.
**Result 3 — NEW (filed as #160):** same arm, only the model cache differs — warm cache refusal
**1.07 s**, empty `MONET_MODEL_CACHE` refusal **33.43 s** after downloading **586,779,294 B**
(`bge-m3/onnx/model_quantized.onnx` 569,694,530 B). An accepted fresh-store start is **0.9 s**, so the
33 s is the provider fetch. `bootstrap.ts` (`embedder-selection` → `store-open`) is **unchanged** by
the PR → the ceiling is decided after the embedder loads, i.e. "decide before opening the store" (the
PR's own headline) is not yet "decide before paying for the provider".
**Result 4 — zero-length store:** a store holding one 0-byte `monet.db` is **served and bootstrapped
on all three builds** (1.11.0 / base / PR HEAD: 0 → 417,792 B) — the PR's zero-length change is about
the pre-engine *decision* (`0` fresh vs `null` unopenable), not about serving; recorded so a later run
does not read the PR title as a serving regression.
**Suite (GR-09, measured this run):** `38/59 PASS + 13 XFAIL + 8 XPASS + 0 STALE + 0 FAIL` (exit 0) —
no new flips (standing XPASS set test23/27/30/33/51/52/53/54); run 121's test42 retirement left no
STALE block.

**Issue:** https://github.com/team-monet/monet/issues/160 (created after a 5-phrasing duplicate search;
read back OPEN, 4,776-char body with the measured numbers → the pilot contract's "narrowly evidenced
issue when no duplicate exists").
**Next wake signal:** a release carrying the ceiling fix (full suite; expect test58 XPASS → update
ISSUES RE-58) · PR #159 HEAD change/merge (re-run the arm pair; re-check the `-shm` residual and the
cold-cache cost) · #160 picked up (re-measure the empty-cache refusal).
**Records:** this file, `artifacts/agent-state.md`, `METRICS.md` run-122 row,
`schema-migration.md` §"Independent verification of the fix HEAD (run 122)". No product source
touched; production store never opened (all probes `-d`).
