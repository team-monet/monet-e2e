
## Run 122 — 2026-09-20 (worker): PR #159's write-free refusal verified on the live path; the refusal's COST measured and filed (#160)

Gate: `NO_NEW_RELEASE 1.11.0` (installed 1.11.0 = npm `latest` 1.11.0, npm time 2026-09-01T10:46Z);
`main` = `d920bc2` (clone in sync 0/0). Shipped `dist/cli.js` still has **0** occurrences of
`readSchemaVersionFromReadonlyConnection` / `walHasBytes` / `storeSchemaCeilingError` /
`readStoredSchemaVersion` / `refusing to open` → the ceiling fix is in no release, RE-58 stays XFAIL.
Signal that changed: PR **#159** (`agent-ops/monet-158-ceiling-shapes`, HEAD `caa9d88`, OPEN + draft,
`Closes #158, Closes #156`) — the fix candidate for the defect run 121 confirmed behaviourally.

**Objective:** re-test the previously blocked high-risk invariant (ceiling refusal must not write the
store it refuses) on the **live `monet start` path**, HEAD vs merge base, and measure what the refusal
costs. Method: `git worktree` at `caa9d88` + `node_modules` symlinks + the existing bundle builder →
`dist/cli.srcaa9d88.js` beside the shipped bundle; probe = 4 sidecar arms (`A_bare_header`,
`B_real_header`, `S1_orphan_wal`, `S2_hot_journal`), each stamped `user_version = 14` vs supported 13,
measuring per arm: main-file sha256/size, sidecar set+sizes, journal_mode, table set, `user_version`,
process state/exit, stderr.

**Result 1 — write-free CONFIRMED (base → PR HEAD).** Base: main file changed on 3/4 arms
(+8192/+12288 B, `delete→wal`, `remote_circle_map` created, hot `-journal` consumed, S1's WAL folded
`uv 0→14`). PR HEAD: **all four arms sha-unchanged, size Δ 0, journal_mode preserved, no new tables,
hot `-journal` (4616 B) preserved, S1's WAL left unfolded (`uv 0`)**. Only residue = the intended
`monet.db.startup-failure.json` + one 32 KB `monet.db-shm` on the orphan-`-wal` arm (the read-only WAL
peek). Refusal text/exit identical on every arm; `user_version` never re-claimed.
**Result 2 — guard:** test58 against the PR HEAD → `RESULT: XPASS` (exit 3), all four arms refuse.
**Result 3 — NEW (filed as #160):** same arm, only the model cache differs — warm cache refusal
**1.07 s**, empty `MONET_MODEL_CACHE` refusal **33.43 s** after downloading **586,779,294 B**
(`bge-m3/onnx/model_quantized.onnx` 569,694,530 B). An accepted fresh-store start is **0.9 s**, so the
33 s is the provider fetch. `bootstrap.ts` (`embedder-selection` → `store-open`) is **unchanged** by
the PR → the ceiling is decided after the embedder loads, i.e. "decide before opening the store" (the
PR's own headline) is not yet "decide before paying for the provider".
**Result 4 — zero-length store:** a store holding one 0-byte `monet.db` is **served and bootstrapped
on all three builds** (1.11.0 / base / PR HEAD: 0 → 417,792 B) — the PR's zero-length change is about
the pre-engine *decision* (`0` fresh vs `null` unopenable), not about serving; recorded so a later run
does not read the PR title as a serving regression.
**Suite (GR-09, measured this run):** `38/59 PASS + 13 XFAIL + 8 XPASS + 0 STALE + 0 FAIL` (exit 0) —
no new flips (standing XPASS set test23/27/30/33/51/52/53/54); run 121's test42 retirement left no
STALE block.

**Issue:** https://github.com/team-monet/monet/issues/160 (created after a 5-phrasing duplicate search;
read back OPEN, 4,776-char body with the measured numbers → the pilot contract's "narrowly evidenced
issue when no duplicate exists").
**Next wake signal:** a release carrying the ceiling fix (full suite; expect test58 XPASS → update
ISSUES RE-58) · PR #159 HEAD change/merge (re-run the arm pair; re-check the `-shm` residual and the
cold-cache cost) · #160 picked up (re-measure the empty-cache refusal).
**Records:** this file, `artifacts/agent-state.md`, `METRICS.md` run-122 row,
`schema-migration.md` §"Independent verification of the fix HEAD (run 122)". No product source
touched; production store never opened (all probes `-d`).

## Run 123 — 2026-09-20 (worker): the derived card TITLE/SLUG verified end-to-end for the first time (RE-60, upstream #117) — the same truncation also eats the contradiction-detail arbitration evidence

Gate: `NO_NEW_RELEASE 1.11.0` (installed literal 1.11.0 = npm `latest` 1.11.0); `main` = `d920bc2`
(clone in sync 0/0); PR #159 still `OPEN`/draft → no bump, no pivot, and run 122's handover signals had
not moved. So the run took the one open **user journey** the suite had never touched that would not
duplicate an upstream report: **issue #117** (re-read directly: `state=OPEN`, author JohnOnLee,
**0 comments**, no fix PR, and `main` `d920bc2` carries the identical regex).

**The defect, measured on the live MCP path (isolated temp store, fresh circle, 8 arms).** `firstLine()`
is `t.trim().split(/\n|(?<!\d)\.|\.(?!\d)/)[0].trim()` (engine.ts:18889) — the source's own comment says
the intent was only version numbers like `0.5.0` — and `title = firstLine(content)`,
`slug = slugify(title)` (engine.ts:18250/18259), so the slug inherits the cut. List titles:
`Updated app/card-image.ts downloadCardImage…` → `Updated app/card-image`;
`Bump vite.config.ts target to es2022…` → `Bump vite`;
`…Svelte mounts App.svelte at the root` → `…mounts App`;
`Deploy the docs to example.ai…` → `Deploy the docs to example`.

**Two amplifiers the issue did not name.** (1) `memory_search` cards carry **no `title` field at all**
(`{id,slug,kind,circle,observationCount}`, mcp-server.md:46) — so for a recalling agent the slug *is*
the whole triage text: `bump-vite` for a memory about `vite.config.ts`. (2) The same helper builds the
arbitration line (`correction: ${firstLine(content)}`, engine.ts:5772), so the `memory_store` ack of a
correction returned `contradiction.detail = "correction: Correction: bump vite"` — the file the
correction names is missing from the evidence an agent arbitrates `accept-new` vs `keep-current` on.
The helper also feeds the next-title (7055/8138), source title (8876) and impeachment evidence (12964)
strings.

**Controls that hold** (a narrow fix must keep all four): `monet-core 0.6.0` survives; a plain sentence
is untouched; a real sentence end (`". "`) still cuts; a newline still cuts. Record-only arms (never
asserted): `Use e.g. a lexicon…` → `Use e` is **not** covered by #117's own suggested regex
(`split(/\n|\.(?=\s|$)/)` still cuts at `e.g. `), and an email-led body truncates to `jane`.

**Records.** RE-60 registered (confirmed, S3, `e2e_test: test61`) + new
`reverse-engineering/title-derivation.md` (8 call sites, card shapes, flip contract). New guard
`tests/test61_re60_title_truncation.py` — XFAIL on first run **and on re-run**, deterministic:
18 checks held, 7 desired-unmet. Registry recounted programmatically: 60 rows = 14 confirmed / 8 fixed
/ 18 source / 13 by-design / 7 removed, 0 `open`, no confirmed-or-fixed row without a test link.

**One FAIL appeared and was classified as HARNESS, not product.** The first suite run this run was
`37/60 PASS + 14 XFAIL + 8 XPASS + 1 FAIL`, the FAIL being `test21 e_retire_after_dismiss` —
**reproduced standalone** (deterministic, so not resource contention): the store step auto-paired that
arm's claim with a structural twin in the same circle (`possible_duplicate_of`, `dismissed_at IS NULL`)
and the retire guard correctly refused ("it carries 1 undismissed pair flag(s)"). The guard's behaviour
is the designed, four-times-reviewed one (engine.ts:7799-7835, #132), so the arm was made deterministic
instead: it now clears those auto-pairs right after the store (GR-06) → test21 standalone
`48 passed / 0 failed`.

**Suite after the fix (GR-09, measured):** `60 tests, 38 PASS + 14 XFAIL + 8 XPASS + 0 STALE + 0 FAIL`.

**No upstream comment posted.** #117 already carries the report and is John's own; a duplicate issue
would be noise, and a public comment is an external action reserved for John's call. The finding is
reported instead — the fix contract is narrow and falsifiable: change the period rule to
`split(/\n|\.(?=\s|$)/)` (which #117 proposes) and all four asserted arms pass while the four controls
still hold.

**Next wake signal:** a release carrying the #155 ceiling fix (expect test58 XPASS → update RE-58) ·
PR #159 HEAD change/merge (re-run the run-122 probe pair) · #160 picked up (re-measure the empty-cache
refusal) · **a release changing `firstLine()`** (grep the shipped bundle for `(?<!\d)\.`) → expect
test61 XPASS, then update RE-60 and re-check the two record-only arms.

**Records:** this file, `artifacts/agent-state.md`, `METRICS.md` run-123 row, `reverse-engineering/ISSUES.md`
RE-60, `reverse-engineering/title-derivation.md`, `tests/test61_re60_title_truncation.py`,
`tests/test21_retire_restore.py` (arm-E fix). No product source touched; production and shared test
stores never written by the probes (isolated temp stores throughout).
