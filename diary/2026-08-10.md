# 2026-08-10 — Run 3: scenario 10 added + concurrency deepened + regression bug fixed

## What was done
1. **Regression found & fixed**: the 9/9 suite from run 2 broke in run 3 on test05
   (`forceNew_present_in_C`). Root cause was a **test bug (GR-06 violation)** — test05 used
   fixed circle names (`e2e-iso-a/b/c`), so state accumulated across runs (circle C had 9
   concepts) and pushed the day's forceNew concept below the search top-5 cutoff.
   Fixed by timestamping circle names (`e2e-iso-*-<ts>`) → back to 9/9.
2. **Scenario 10 new**: dedup growth observation (API + DB layers).
3. **Scenario 9 deepened**: burst writes (4 processes × 5 parallel writes) in 2 sub-phases.

## Run summary
| Test | Scenario | Result | Notes |
|------|----------|--------|-------|
| test01_handshake.py | 1 | 5/5 PASS | regression |
| test02_store_search.py | 2 | 6/6 PASS | regression |
| test03_korean_search.py | 3 | 3/3 PASS | regression |
| test04_cross_session.py | 4 | 4/4 PASS | regression |
| test05_circle_isolation.py | 5 | 9/9 PASS | **GR-06 bug fixed** (fixed circles → timestamped) |
| test06_contradiction.py | 6 | 8/8 PASS | regression |
| test07_schema_migration.py | 7 | 10/10 PASS | regression |
| test08_repair_regression.py | 8 | 6/6 PASS | regression |
| test09_concurrency.py | 9 | **15/15 PASS deepened** | burst phase added |
| test10_dedup_growth.py | 10 | **11/11 PASS new** | dedup growth observation |

**Suite total: 10/10 passing, 77 assertions (59 → 77), 0% failure**

## Finding 1 — test05 re-run bug (GR-06 re-confirmed)
Fixed-circle-name tests accumulate state across runs, and **top-N search assertions break on
ranking cutoff**. That's a test isolation bug, not a product bug.
→ Applied GR-06 (fresh circle per run) to test05. A debug probe (`probe_c.py`) confirmed the
9 accumulated concepts in circle C before the fix.

## Finding 2 — dedup works under concurrency (scenario 9 deepened)
4 server processes × 5 near-identical template stores (20 total, parallel) → **all merged into a
single concept** (unique=1, wall 2.2s, 0 lock errors). No race when multiple writers attach to
the same concept concurrently; dedup is safe across writers.

## Finding 3 — dedup threshold is aggressive (product observation)
Template-similar sentences differing only by noun (e.g. "a X crossed Y at dawn") still merge —
20 semi-distinct sentences collapsed to 5 concepts (1 per writer). Truly distinct sentence
STRUCTURE is required to stay separate (20 hand-written sentences → 20 unique concepts).
The actual dedup threshold is quite high — from a user perspective "similar things merge"
may feel too aggressive. Product review candidate.

## Finding 4 — dedup growth pattern (scenario 10)
- First store `action=created`; later near-identical stores `action=attached` (same conceptId)
- observationCount grows 1:1 with stores; body accumulates every observation
- DB layer: `observation_segments` == observations (1:1); `observation_tokens` accumulate
  per observation (5 obs → 45 tokens)
- Clearly distinct content stays in a separate concept (control passes)

## Metric changes
Tests 9 → 10, coverage 100% maintained (all 9 matrix scenarios + scenario 10),
assertions 59 → 77, failure rate 0%.

## Next steps
1. Scenario 10 extension: observe `needsSynthesis`/`memory_synthesize` transition after dedup;
   measure segments/tokens growth curve at 50+ observations.
2. Scenario 9 new angle: read+write mixed-load latency distribution; busy-timeout-exceeded path
   is hard to force — keep observation-focused.
3. Product follow-up candidate: find where the dedup similarity threshold is defined in source.

## Meta
- Patched `monet-e2e-testing` skill: test10 added, test09 burst phase, test05 GR-06 fix, dedup findings.
- Harness additions: `test10_dedup_growth.py`; modified: `test05_*.py`, `test09_concurrency.py`.
- Production Monet MCP server/DB not touched — everything ran in `-d` isolated dirs.
