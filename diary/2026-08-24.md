# 2026-08-24 — Run 68 (E2E worker)

Release gate: `NO_NEW_RELEASE 1.7.1` → normal worker loop (not supervisor pivot).
Open-issue queue had ONE item: **monet-e2e#27** (triage + register upstream #88/#89
→ RE-53/54, both structural → L2). That is DIRECTION run-13 priority #1, so this
run executed it.

## What was done (all in repo, English, PROD `~/.monet` untouched GR-01)

1. **Triage upstream team-monet/monet #88 + #89** (both found by Codex review on
   the #87 tauMargin PR; both structural — no MCP/CLI surface, so `source` status
   and L2-queue, NOT E2E-XFAIL-able):

   - **#88 (sev:minor, area:store) → RE-53 (S3).** A store that crosses the
     ambiguity gate (#86's third attach outcome: below `tauMargin` nothing is
     written and the caller resolves via `attachTo`/`forceNew`) is counted by NO
     resolution event. The ask throws from inside the write transaction before
     `recordResolutionEvent` (deliberate — makes "nothing written" structural —
     but the throw already rolled back, and `resolution_events.observation_id` is
     NOT NULL so an ask cannot be recorded anyway); the retry records
     `direct-attach`/`force-new`, which `DECIDED_RESOLUTION_MODES` excludes (bulk/
     consolidation attachTo dilution rationale). Two independently-defensible
     exclusions together drop the whole exchange → every rate ÷ `decidedTotal`
     (fork rate, duplicate-emission rate) is measured only over stores the gate
     let through silently: a selection bias toward exactly the population the gate
     exists to shrink. Fix is wider than #87 (schema change + caller-supplied
     retry signal), so routed L2.
   - **#89 (sev:major, area:store, John-ratified 08-23) → RE-54 (S2).** The 12
     `packages/core/scripts/measure-*.ts` calibration scripts reimplement the
     decisions they justify instead of calling them (0/12 call
     `scoreNativeConceptsByObservation`, 1/12 call `resolveIncoming`). A
     reimplementation silently drifts toward making the constant look derived —
     Codex found 11 divergences across 4 rounds, all numerically inert by luck,
     and `STARTER_SUITE` (98 obs/98 segs, `probe.vecs[0]==probe.whole`) cannot
     exhibit the probe-unit defect (so a fidelity test is not the fix). Fix = a
     shared driver calling the engine's scorer+decision, then re-derive
     tauMargin/tauAttach/edgeSimMin/NATIVE_SCORE_FLOOR/LEXICAL_BOOST/
     reliableSegmentTokens per profile.

2. **Registered both in ISSUES.md** (`source` status, `e2e_test —`, severity S3/S2)
   and added a maintenance-note changelog entry.

3. **Added owning-module paragraphs**: `dedup-resolution.md` (RE-53, the ambiguity-
   gate resolution-event gap) and `search-pipeline.md` (RE-54, calibration-script
   drift). Both reference the engine functions in `retrieval.ts`/`resolution.ts`.

4. **Routed both to the L2 structural section** of `L2-code-fix-queue.md`
   (structural section 3→5 rows: RE-42/46/50 + RE-53/54), with design options.

5. **Test changes: none** — both structural, per DIRECTION complete-definition.
   No suite run needed (no code/test touched, and DIRECTION #3 halts coverage work;
   the green 13-XFAIL matrix from run 67 is unchanged and nothing in this run could
   drift it).

## Results

- RE registry 52 → 54 issues (RE-53 `source` S3, RE-54 `source` S2). No behavior
  status flipped (structural only). Suite/metrics/code-coverage all unchanged
  (83.8% line coverage held; DIRECTION pipeline COMPLETE).
- monet-e2e#27's complete-definition satisfied (ISSUES rows + module paragraphs +
  L2 routing); issue to be closed with a completion record.

## Next steps

- Worker: the only open-issue item is served; matrix is green and sustained at
  37/55 + 13 XFAIL + 5 XPASS (run 67). Next real advance waits on the next
  @team-monet/monet release (re-verify the 13 XFAIL) or a new upstream batch to
  triage. Keep the green suite.
- Supervisor: host-memory-pressure check on the standing `monet` MCP PID +
  test09 burst contention (run-64/65 flake, clean in 66/67) remains the one open
  operational item.

## Meta

- Worker: E2E/RE triage. Prod `~/.monet` untouched (GR-01/07). Docs/commits English.
---

# 2026-08-24 — Run 69 (E2E worker, stability re-verify)

Release gate: `NO_NEW_RELEASE 1.7.1` → normal worker loop (not supervisor pivot).
Open-issue queue: **empty** (monet-e2e#27 closed in run 68). DIRECTION #1 (the
#88/#89 → RE-53/54 triage) was already fully served by run 68; priorities #2
(13 XFAIL matrix green) and #3 (coverage pipeline COMPLETE) govern. Last full
suite was run 67 (green), so this run re-runs the suite to confirm the matrix
holds on installed 1.7.1.

## What was done (all in repo, English, PROD `~/.monet` untouched GR-01)

1. **Full-suite re-run** (`harness/run_suite.sh`, isolated `~/.monet-test`):
   **36/55 pass + 13 XFAIL + 5 XPASS + 0 genuine FAIL** (exit 0). XFAIL set
   (RE-07/17/21/26/04/33/32/44/45/48/49/51/52) identical to run 67; XPASS set
   (RE-19/43/42/50/47, all `fixed`) identical; **no drift, no new flip**.
2. **test09 concurrency flake reconfirmed → environmental, NOT a regression.**
   Ran 3 times total (once in-suite, twice standalone). Every run produced the
   SAME fingerprint: exactly **one of the two burst phases** fails with exactly
   one store (`ok=19/20`), the other phase clean `20/20`, and which phase fails
   is random across runs:
   - In-suite: burstA FAIL (19/20, unique=2), burstB PASS.
   - Standalone 1: burstA PASS (20/20, unique=1), burstB FAIL (19/20, unique=20).
   - Standalone 2: burstA FAIL (19/20, unique=2), burstB PASS.
   Every run: `PASS no_lock_errors` (locks=none), `PASS post_doctor_integrity`
   (ok), cross-visibility ok. The single dropped write is the model-load memory
   spike each of the 4 parallel server processes causes at startup (~1 GB
   bge-m3), contending with the standing `monet` MCP PID **93405** (holds
   `~/.monet/monet.db`); burst wall A/B 13–18 s. This matches the documented
   contention flake (run 64/65 reproduced, run 66/67 clean) — ping-ponging
   between A and B per run is the expected signature. Not a product regression
   (test09's deterministic 2-process phase and all burst invariants pass).
3. **No test/metric/code-coverage change** — stability-only run. Line coverage
   stays saturated at 83.8% (DIRECTION #3 halts coverage work; not re-measured).

## Results

- Suite green and sustained: 36/55 + 13 XFAIL + 5 XPASS + 0 genuine FAIL,
  matrix byte-identical to run 67.
- test09 flake: reconfirmed environmental (memory contention), still the one
  open OPERATIONAL item — supervisor to check host memory pressure + the
  standing `monet` MCP PID. Flake likelihood seems elevated today (3/3 burst
  phases hit it across 3 runs vs clean in 66/67), suggesting current host
  memory is tighter than a week ago.

## Next steps

- Worker: matrix green, queue empty, no release pending — nothing additive
  until the next @team-monet/monet bump (re-verify the 13 XFAIL) or a new
  upstream batch to triage. Keep the green suite (re-run on demand).
- Supervisor: prioritize the host-memory-pressure check on the standing
  `monet` MCP PID + test09 burst contention — flake frequency has risen.

## Meta

- Worker: E2E/RE sustain. Prod `~/.monet` untouched (GR-01/07). Docs/commits English.