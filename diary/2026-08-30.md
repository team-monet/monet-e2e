# 2026-08-30 — Run 78/79 (E2E worker, 1.9.1 re-verify + RE-55 declare disclosure)

Release gate: `NO_NEW_RELEASE 1.9.0` (the check reports installed 1.9.0; `.release_handled=1.9.1`
because supervisor run #78 marked the release — but PROD binary was still 1.9.0, and the 1.9.1
re-verify had NOT been run). npm latest = 1.9.1 (gitHead ab8ef40). Followed DIRECTION #1
(1.9.1 re-verify on an isolated prefix, run-61 precedent) — this is the first real 1.9.1
measurement on the E2E suite. Run #77 (08-29) confirmed green on installed 1.9.0.

## Release reconciliation (why the gate looked "no new release")
`monet_release_check.sh` prints `NO_NEW_RELEASE <installed>` when npm latest <= `.release_handled`.
Since `.release_handled=1.9.1` (supervisor marked it during run-78 pivot) the gate no longer
triggers a pivot — but the PROD binary under `~/.local/share/monet-node22` is STILL 1.9.0 and the
1.9.1 re-verify had not run. This is a release-gate blind spot: the pivot marks handled before
the worker re-verifies. Resolved by building an isolated `~/.monet-test/prefix-191` (npm global
install @team-monet/monet@1.9.1 to that prefix) and pointing `MONET_CLI` at it. No prod store or
prod binary touched (GR-01).

## What was done (isolated ~/.monet-test only; PROD ~/.monet untouched GR-01)
1. Built `~/.monet-test/prefix-191` with node@22 + `--build-from-source=false` (~/.monet/models
   cache reused, no re-download). `MONET_CLI=<prefix-191>/.../dist/cli.js`.
2. Full suite on isolated 1.9.1 → **38/56 pass + 10 XFAIL + 8 XPASS + 0 FAIL (exit 0)**.
3. **Flip confirmed exactly as DIRECTION/#30 predicted:**
   - **test27 (RE-21) XFAIL→XPASS** — graphDensity now excludes possible_duplicate_of edges
     (a71d8cb #98/#56); reclassified confirmed→fixed in ISSUES.md.
   - **test54 (RE-51) XFAIL→XPASS** — checkpoint receipt now carries landedInArchivedCircle +
     guidance on an archived landing (4efa59a #101/#81); reclassified confirmed→fixed.
   - **test20 dashboard FAIL fixed** — test20's ARM F pinned the OLD graphDensity formula
     (edgesLive/concepts); RE-21's fix changed the contract to (edgesLive−dup_edges)/concepts.
     Updated test20 to count dup edges from the /api/graph edges array and assert the new
     contract ([density=1.5 = (5−2)/2 expired=1.5]); re-verified 33 passed, 0 failed.
4. **New XFAIL test56 (RE-55,** declare*-archived disclosure**): `memory_declare` writes a
   principle into an ARCHIVED circle with NO disclosure — receipt keys
   {action,advisories,circle,conceptId,species} carry no archived/guidance/landedInArchivedCircle
   signal, and the DB concepts row kind='principle' lands in the archived circle name (outside
   store-wide recall). RE-21's #101 note names declare() and reassignCircle as REMAINING
   undisclosed gaps (the checkpoint sibling is fixed). Confirmed XFAIL (exit 2) on 1.9.1.
5. **RE-55 registered** in ISSUES.md (confirmed, test56, S3). reassignCircle probed on 1.9.1
   (moves a concept between circles, no archived-disclosure on transfer) — per #31 triage this
   is a lower-severity STRUCTURAL gap → routed to L2 source queue, not an XFAIL (no clean MCP
   contract to pin).
6. L2 dequeue for RE-21 (S4) and RE-51 (S2) signaled to supervisor (DIRECTION re-write is
   supervisor-owned; this worker records the flip + dequeue request in diary).
7. GitHub #30 and #31 closed with full verification notes.

## Results
- Suite green on isolated 1.9.1: **38/56 pass + 10 XFAIL + 8 XPASS + 0 FAIL**.
  The 8 XPASS = fixed set (RE-19/43/42/50/47/04/21/51); 10 XFAIL = RE-07/17/26/32/33/44/45/48/49/55.
  No regression in the other 9 tracked XFAIL.
- ISSUES matrix now: confirmed 10 (RE-07/17/26/32/33/44/45/48/49/55), fixed 8
  (RE-04/19/42/43/50/47/21/51), source 17, removed 7, by-design 13 = 55 exact.

## Next steps
- Supervisor: reflect RE-21/RE-51 dequeue in DIRECTION L2 queue (active S2 now RE-26/44/45 +
  RE-51-dequeued; S4 RE-32 + RE-21-dequeued); note the release-gate blind spot (pivot marks
  handled before the worker re-verifies) and consider triggering the worker on `.release_handled
  != installed`.
- Worker: keep the green suite idle; next re-verify on the next @team-monet/monet bump
  (first flip candidates RE-17/test24 and RE-55/test56 archived-circle refuse/disclosure).
- John/prod: PROD `~/.local/share/monet-node22` is still on 1.9.0 — server-ops upgrade to
  1.9.1 is a John action (worker only re-verified on the isolated prefix; did not touch prod).

## Meta
- Worker: E2E 1.9.1 re-verify. Prod `~/.monet` untouched (GR-01). Docs/commits English.