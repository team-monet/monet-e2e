# 2026-09-01 — run 84 (worker, 1.10.0 release re-verify)

## Focus
DIRECTION #1: **`@team-monet/monet` 1.10.0 isolated re-verify** (npm latest =
1.10.0, tag v1.10.0, 08-31T06:25). `.release_handled=1.10.0` was already aligned
by run-83 supervisor, so the release gate reads `NO_NEW_RELEASE 1.9.0`
(latest==handled) → worker loop, and the marker being set but the re-verify not
yet run is exactly the run-78/79 blind-spot the DIRECTION calls out. This run
built the isolated `prefix-110` and ran the full suite against it.

## What was done
- **Built isolated `~/.monet-test/prefix-110`** (node@22, `--build-from-source=false`,
  `MONET_MODEL_CACHE` reused → no model re-download). package.json version 1.10.0.
- **Full suite vs 1.10.0 → 38/56 pass + 10 XFAIL + 8 XPASS + 0 FAIL (exit 0).**
  Tally byte-identical to runs 79/80 on 1.9.1, and to the run 73/77 1.9.0
  baseline shape. **No flip in either direction** — 1.10.0 resolves no tracked
  RE issue and introduces no regression.
- **Both predicted flip candidates REJECTED (negative confirmed):**
  - **RE-55/test56 (declare archived disclosure) still XFAIL.** Source check:
    #128 homeCircle (47d48567) names the circle a rule/skeleton member is HOMED
    in for cross-breadth delivery (`*`/global) only — it does NOT add an
    archived-declare disclosure signal. test56 flakes nothing → RE-55 stays
    `confirmed`.
  - **RE-56 (reassignCircle archived disclosure) stays `source`, L2.** The
    reassignCircle MCP handler on `main` (mcp-server.ts:2162-2308) still emits
    `{action, conceptId, fromCircle, toCircle, observationsMoved}` + `DENY
    RELOCATED` but NO archived/`landedInArchivedCircle` clause when `toCircle`
    is archived; #128 doesn't touch this path.
  - ISSUES.md RE-55/RE-56 rows annotated with the 1.10.0 re-check verdict.
- **RE-27-adjacent #131/#133 rule-retire surface screened (DIRECTION #2, small):**
  1.10.0 MCP tool-name set identical to 1.9.0 (`conformance_ask`/`conformance_answer`
  only; no rule-retire/refuse surface added) → RE-27 `by-design` stands, no new RE.
- No new test (DIRECTION halts coverage/new-RE work past RE-55/56; bump only
  triggers re-suite, and this bump did that once).

## Metrics
- Tests 56, scenario coverage 100%, line coverage 83.8% (no coverage run —
  DIRECTION #3 COMPLETE). Zero genuine FAIL.
- XFAIL 10 steady (each confirmed RE): RE-07/22, RE-17/24, RE-26/29, RE-32/32,
  RE-33/31, RE-44/34, RE-45/36, RE-48/38, RE-49/41, RE-55/56.
  XPASS 8 steady (the `fixed` set): RE-19/23, RE-21/27, RE-04/30, RE-42/51,
  RE-43/33, RE-47/53, RE-50/52, RE-51/54. test25/26/28/55 (removed guards) SKIP.
  Matrix consistency check passes (confirmed→XFAIL, fixed→XPASS, source/by-design→`—`).

## Next steps
- **John/server-ops**: PROD `~/.local/share/monet-node22` binary is now TWO
  versions behind (1.9.0 → 1.10.0). Isolated re-verify passed clean; PROD
  upgrade is the server-ops action. After upgrade, `.release_handled` stays
  1.10.0 (already correct).
- **Worker**: sustain — next release bump is the only re-verify trigger. First
  flip candidates for the NEXT bump remain RE-17/test24 (storeInternal archived
  refuse) + RE-55/test56 (declare archived disclosure).
- **L2 queue unchanged**: RE-56(S3) + RE-53/46/54 structural set; RE-55(confirmed/XFAIL)
  stays until disclose-or-refuse ships.