# 2026-08-15 — Run 28: RE-21 (graphDensity counts possible_duplicate_of) → CONFIRMED (XFAIL)

## What was done
1. **RE-issue verification loop** (priority per pilot prompt): no open GitHub
   issues. Scanned `reverse-engineering/ISSUES.md` for `open` +
   behaviorally-verifiable issues and selected **RE-21** (S4 — dashboard
   `graphDensity` counts `possible_duplicate_of` edges) — the dashboard
   module's last open issue and the cleanest deterministic MCP+dashboard
   reproduction among the remaining open issues (RE-26 S2 needs a busy-store
   approach; RE-04 S3's lexical-arm is fragile to pin via result ordering;
   RE-05 S4 is blocked by RE-30 since source_sync is broken on macOS; RE-09/
   RE-31 are structural, not behavioral).
2. **Wrote `tests/test27_re21_graphdensity_dup_edges.py`** (XFAIL contract):
   store 2 similar same-circle concepts (shared token → bidirectional
   `possible_duplicate_of` pair flag), read `/api/graph` via the dashboard,
   assert the DESIRED contract `graphDensity == (edgesLive − dup_edges)/concepts`
   (structural density, excluding dedup pair-flag edges). Exit 2 while the
   bug is present.
3. **Result: XFAIL (exit 2)** — measured `edgesLive=7` (2 possible_duplicate_of
   + 5 structural/about edges), `concepts=2`, `graphDensity=3.5` (= 7/2) vs
   `structural_density=2.5` (= (7−2)/2). The 2 dup edges inflate density by
   exactly `dup_edges/concepts` = 1.0 — a **40% overstatement** on this tiny
   2-concept store.
4. **Updated ISSUES.md** (RE-21 → `confirmed`, `e2e_test=test27`) and
   **dashboard.md** (RE-21 E2E note appended).

## Findings
- **RE-21 CONFIRMED (S4).** `graphDensity = liveEdges/concepts` counts
  `possible_duplicate_of` edges. On the 75 MB E2E store that was ~3.2% of
  edges (184/5751); on a sparse store with a single dup pair it is a 40%
  overstatement. "Graph density" overstates structural density.
- Minor observation (not investigated further, no new issue): `counts.
  possibleDuplicatePairs` returned `2` for a single bidirectional pair
  (2 concepts) — the count appears to be per-directed-edge (or per-concept-in-
  pair) rather than per-pair, while the field name says "pairs".

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |
| test22 (RE-07) | XFAIL |
| test23 (RE-19) | XPASS (already fixed + closed) |
| test24 (RE-17) | XFAIL |
| test25 (RE-30) | XFAIL |
| test26 (RE-23) | XFAIL |
| test27 (RE-21) | XFAIL (new) |

**Suite total: 21/27 passing, 5 xfail, 1 xpass, 392 assertions, 0% failure**

## Metric changes
- Tests 26 → 27, coverage 100% (unchanged), failure 0% (unchanged).
- Assertions 385 → 392 (+7 setup checks in test27).
- RE-21 status open → confirmed (e2e_test=test27).

## Next steps
1. Convert the next open behavioral issue, in severity order: RE-26 (S2,
   gate_events no retention — busy-store approach), RE-04 (S3, Latin-only
   lexical arm — needs a deterministic ordering probe), RE-24 (S2,
   updateSource access mutation — confirm CLI/MCP reachability first),
   RE-05 (S4, source skip floor — blocked by RE-30 until the macOS rename
   fix ships).
2. Cross-check the contradiction + circle minified docs against their
   readable-TS counterparts (run-27 next-step #2).
3. Re-check RE-30/RE-23/RE-31 on the next @team-monet/monet bump.

## Meta
- Guardrails honored: isolated `mkdtemp` store + local dashboard only; prod
  `~/.monet/monet.db` untouched; no `mcp__monet__*` calls; all docs English.
- Files changed: `tests/test27_re21_graphdensity_dup_edges.py` (new),
  `reverse-engineering/ISSUES.md` (RE-21 confirmed), `reverse-engineering/
  dashboard.md` (RE-21 E2E note), `METRICS.md`, this diary.


---

# 2026-08-15 — Run 29 (reverse-engineering): statement tracing + embed window guard + lifecycle edges + skeleton mirror

## What was done
1. Resumed the readable-TS documentation pass (run-27 next-step #1, the first item on
   the list: statement-trace + segmenter/budget + lifecycle-edges + skeleton-mirror).
   Read `src/statement-trace.ts`, `src/observation-segmenter.ts`, `src/embed-budget.ts`,
   `src/script-gate.ts`, `src/lifecycle-edges.ts`, `src/skeleton-mirror.ts`, the window
   guard + `ContentExceedsEmbedderWindowError` in `src/engine.ts`, and the contract tests
   (`statement-trace`, `write-budget`, `query-budget`, `segment-budget-travel`,
   `store-busy`).
2. **Documented 4 modules** (all readable TS, all previously undocumented):
   - `statement-trace.md` — the SQL hang / slow-query hunting instrument (in-flight marker
     written BEFORE the statement + slow log after; #145 wedge diagnosis).
   - `embed-window-guard.md` — the bounded-retrieval budget (RELIABLE_EMBED_TOKENS 280
     advisory@write / enforced@segmenter) + write/query over-window refusal + Latin-script
     gate (0.2 tolerance).
   - `lifecycle-edges.md` — the normative substrate (derivation/provenance/supersession
     edges + ratifications, separate append-only tables, 4-gate extraction battery).
   - `skeleton-mirror.md` — materialized standing-file stale detection (3 stale reasons).
3. Updated `METRICS.md` (core row + 4 module-inventory entries + 30 param rows),
   `ISSUES.md` (+RE-33, RE-34).

## Findings
- **The budget/window-guard subsystem is now fully cross-checked at readable source.**
  `RELIABLE_EMBED_TOKENS=280` is advisory at the write boundary (refusing there would turn
  away legitimate single-claim writes) and enforced only in the segmenter
  (`segmentTokenBudget = min(reliable, window)`). The hard boundary for a write/query is
  the model's `inputWindow` (`assertWithinEmbedderWindow`, cheap, pre-embed), with
  idempotency-receipt lookup ordered BEFORE the guard so a committed operationId replays as
  a no-op even over-window. `storeSource` bypasses the write budget (no author to retry).
  All three model-travel invariants (segment budget, card-emission floor, edgeSimMin) are
  pinned by `segment-budget-travel.test.ts`.
- **Two new issues:**
  - **RE-33 (S3, open)** — `slow-queries.jsonl` is write-only. `readInflightStatements`
    gives the in-flight marker a consumer (the lock-contention holder naming in
    `storage.ts`), but nothing reads/surfaces the slow log — the retrieval-degradation
    diagnosis it exists to provide has no doctor/CLI/MCP path yet.
  - **RE-34 (S4, by-design)** — lifecycle-edge cross-circle is checked at creation but NOT
    maintained: a later `reassignCircle`/`moveConcept` moves one endpoint into another
    circle and leaves the edge standing with a `circle` that no longer names both
    endpoints. Source documents `circle` as provenance, never a live locality index; the
    read API (`getLifecycleEdges`) does not filter by circle, so the stance is internally
    consistent — recorded for visibility as a latent footgun.
- `statement-trace.ts` is a notably well-engineered instrument: env-gated
  (`MONET_TRACE_SQL=1`, off by default, one boolean test cost), a frame stack (not a slot)
  so a hang in COMMIT still shows the enclosing transaction, one file per connection,
  truncate-then-write to avoid stale tails, 0600 with explicit fchmod, all 10 SQL mouths
  traced (incl. exec/pragma/async backup), and the reader is total/never-throws.

## Metric changes
- Modules documented: **10 → 14** (statement-trace, embed-window-guard, lifecycle-edges,
  skeleton-mirror).
- Thresholds/params identified: **130 → 160** (+30).
- Issues found: **32 → 34** (+RE-33, RE-34).

## Next steps
1. Continue the readable-TS pass on the remaining undocumented engine.ts subsystems:
   lifecycle-edges is done; remaining candidates are statement-trace's companion
   `diagnostics.ts` (lifecycle-edge integrity checks), the write/query-budget is now
   documented but the `source-chunker.ts` (which owns the source chunk budget) and the
   materialize command wiring (skeleton-mirror's write half) are not yet.
2. Cross-check the remaining minified-bundle docs (contradiction, circle, search, schema)
   against their readable-TS counterparts for drift — contradiction + circle were the
   run-27/run-28 next-step #2 items still open.
3. E2E verification loop: RE-33 is behaviorally verifiable (enable MONET_TRACE_SQL=1, run
   a slow query, assert no tool surfaces the slow log) — candidate for a future XFAIL test;
   RE-26 (gate_events retention) and RE-04 (Latin-only lexical arm) remain the top
   behavioral open issues.
4. Re-check RE-30/RE-23/RE-31/RE-33 on the next @team-monet/monet bump.

## Meta
- Guardrails honored: source read-only; prod `~/.monet/monet.db` untouched; no
  `mcp__monet__*` calls; proprietary detail kept to behavior/params/issue summaries (no
  bulk TS quoting); table pipes escaped (`\|`); all docs English.
- Files changed: `reverse-engineering/statement-trace.md` (new),
  `reverse-engineering/embed-window-guard.md` (new),
  `reverse-engineering/lifecycle-edges.md` (new),
  `reverse-engineering/skeleton-mirror.md` (new), `reverse-engineering/METRICS.md`,
  `reverse-engineering/ISSUES.md`, this diary.


---

# 2026-08-15 — Run 30: RE-24 (updateSource silently de-authorizes the host) → CONFIRMED (XFAIL)

## What was done
1. **RE-issue verification loop** (priority per pilot prompt): no open GitHub issues
   (only the run-20 issue #1, already closed). Scanned `reverse-engineering/ISSUES.md`
   for `open` + behaviorally-verifiable issues. Selected **RE-24** (S2 —
   authorization footgun): `updateSource` marks identity fields immutable but leaves
   `access` mutable, so a host can silently de-authorize the caller that registered
   the source. Chosen over the other open behavioral issues because it is the
   highest-severity open behavioral issue with a clean, deterministic reproduction
   (RE-26 gate_events-retention needs a busy-store approach; RE-04 Latin-only
   lexical arm is fragile to pin via result ordering; RE-33 slow-log write-only
   needs a forced >1s query; RE-05 is blocked by RE-30).
2. **Confirmed reachability**: the CLI `source update` command exists
   (`monet source --help` → `update <source-id> — Update mutable source
   configuration`) and exposes repeatable `--allow-caller`/`--allow-project`
   flags that REPLACE each access list — so RE-24 is E2E-observable via CLI + MCP,
   not a core-API-only footgun.
3. **Wrote `tests/test28_re24_update_access_mutation.py`** (XFAIL contract),
   reusing the test26 pattern (git worktree + `source add` + HOME redirect for
   RE-29 isolation + MCP `source_list` control):
   - CONTROL: register a repo-md source with `--allow-caller CALLER`, then
     `source_list` under the acting identity returns 1 source (authorized).
   - PROBE: `source update <id> --allow-caller ATTACKER` (removes CALLER), then
     re-check `source_list` under CALLER's identity.
   - DESIRED contract: the update must refuse, warn, or leave the source visible —
     i.e. NOT silently self-de-authorize.
4. **Result: XFAIL (exit 2)** — `source update` returns rc=0 (`Updated source …`,
   no warning), `allowed_caller_ids_json` becomes `["e2e-attacker"]` (CALLER gone),
   and the acting caller's `source_list` then returns `[]` (silently filtered by
   `authorizeSource`). 5/5 setup checks passed.

## Findings
- **RE-24 CONFIRMED (S2).** The CLI `source update --allow-caller <other>` REPLACES
  the caller list with no warning and no refusal, silently de-authorizing the very
  host that registered/syncs the source. The acting caller's next `source_list`
  returns `[]` and every `source_status`/`source_path`/`source_sync` would return
  the non-disclosing "source is unavailable". The footgun is real and reachable via
  the registry-only CLI (no need to construct MonetCore directly).
- Minor harness note: `sqlite3 db "SELECT … WHERE id=?;" <id>` does NOT bind the
  positional parameter in this environment (returns empty) — the test uses a
  `LIMIT 1` query instead (single-source isolated store).

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |
| test22 (RE-07) | XFAIL |
| test23 (RE-19) | XPASS (already fixed + closed) |
| test24 (RE-17) | XFAIL |
| test25 (RE-30) | XFAIL |
| test26 (RE-23) | XFAIL |
| test27 (RE-21) | XFAIL |
| test28 (RE-24) | XFAIL (new) |

**Suite total: 21/28 passing, 6 xfail, 1 xpass, 397 assertions, 0% failure**

## Metric changes
- Tests 27 → 28, coverage 100% (unchanged), failure 0% (unchanged).
- Assertions 392 → 397 (+5 setup checks in test28).
- RE-24 status open → confirmed (e2e_test=test28).

## Next steps
1. Convert the next open behavioral issue, in severity order: RE-26 (S2,
   gate_events no retention — busy-store approach), RE-04 (S3, Latin-only lexical
   arm — needs a deterministic ordering probe), RE-33 (S3, slow-queries.jsonl
   write-only — MONET_TRACE_SQL=1 + a forced slow query, assert no doctor/CLI/MCP
   surface reads the log), RE-32 (S4, livingModelCard drops the ranking score —
   clean via `memory_overview`).
2. Cross-check the remaining minified-bundle docs (contradiction, circle, search,
   schema) against their readable-TS counterparts for drift (run-27/28 next-step
   #2 still open).
3. Re-check RE-30/RE-23/RE-24/RE-31/RE-33 on the next @team-monet/monet bump.

## Meta
- Guardrails honored: isolated `mkdtemp` store + `HOME` redirect; prod
  `~/.monet/monet.db` untouched (asserted `source_storage_isolated_via_home`);
  no `mcp__monet__*` calls; all docs English.
- Files changed: `tests/test28_re24_update_access_mutation.py` (new),
  `reverse-engineering/ISSUES.md` (RE-24 confirmed),
  `reverse-engineering/sources-sync.md` (RE-24 E2E note), `METRICS.md`, this diary.
