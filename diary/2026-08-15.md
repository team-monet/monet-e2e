# 2026-08-15 — Run 28: RE-21 (graphDensity counts possible_duplicate_of) → CONFIRMED (XFAIL)

## What was done
1. **RE-issue verification loop** (priority per pilot prompt): no open GitHub
   issues. Scanned `reverse-engineering/ISSUES.md` for `open` +
   behaviorally-verifiable issues and selected **RE-21** (S4 — dashboard
   `graphDensity` counts `possible_duplicate_of` edges) — the dashboard
   module's last open issue and the cleanest deterministic MCP+dashboard
   reproduction among the remaining open issues (RE-26 S2 needs a busy-store
   approach; RE-04 S3's lexical-arm is fragile to pin via result ordering;
   RE-05 S4 is blocked by RE-30 since source_sync is broken on macOS; RE-09/
   RE-31 are structural, not behavioral).
2. **Wrote `tests/test27_re21_graphdensity_dup_edges.py`** (XFAIL contract):
   store 2 similar same-circle concepts (shared token → bidirectional
   `possible_duplicate_of` pair flag), read `/api/graph` via the dashboard,
   assert the DESIRED contract `graphDensity == (edgesLive − dup_edges)/concepts`
   (structural density, excluding dedup pair-flag edges). Exit 2 while the
   bug is present.
3. **Result: XFAIL (exit 2)** — measured `edgesLive=7` (2 possible_duplicate_of
   + 5 structural/about edges), `concepts=2`, `graphDensity=3.5` (= 7/2) vs
   `structural_density=2.5` (= (7−2)/2). The 2 dup edges inflate density by
   exactly `dup_edges/concepts` = 1.0 — a **40% overstatement** on this tiny
   2-concept store.
4. **Updated ISSUES.md** (RE-21 → `confirmed`, `e2e_test=test27`) and
   **dashboard.md** (RE-21 E2E note appended).

## Findings
- **RE-21 CONFIRMED (S4).** `graphDensity = liveEdges/concepts` counts
  `possible_duplicate_of` edges. On the 75 MB E2E store that was ~3.2% of
  edges (184/5751); on a sparse store with a single dup pair it is a 40%
  overstatement. "Graph density" overstates structural density.
- Minor observation (not investigated further, no new issue): `counts.
  possibleDuplicatePairs` returned `2` for a single bidirectional pair
  (2 concepts) — the count appears to be per-directed-edge (or per-concept-in-
  pair) rather than per-pair, while the field name says "pairs".

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |
| test22 (RE-07) | XFAIL |
| test23 (RE-19) | XPASS (already fixed + closed) |
| test24 (RE-17) | XFAIL |
| test25 (RE-30) | XFAIL |
| test26 (RE-23) | XFAIL |
| test27 (RE-21) | XFAIL (new) |

**Suite total: 21/27 passing, 5 xfail, 1 xpass, 392 assertions, 0% failure**

## Metric changes
- Tests 26 → 27, coverage 100% (unchanged), failure 0% (unchanged).
- Assertions 385 → 392 (+7 setup checks in test27).
- RE-21 status open → confirmed (e2e_test=test27).

## Next steps
1. Convert the next open behavioral issue, in severity order: RE-26 (S2,
   gate_events no retention — busy-store approach), RE-04 (S3, Latin-only
   lexical arm — needs a deterministic ordering probe), RE-24 (S2,
   updateSource access mutation — confirm CLI/MCP reachability first),
   RE-05 (S4, source skip floor — blocked by RE-30 until the macOS rename
   fix ships).
2. Cross-check the contradiction + circle minified docs against their
   readable-TS counterparts (run-27 next-step #2).
3. Re-check RE-30/RE-23/RE-31 on the next @team-monet/monet bump.

## Meta
- Guardrails honored: isolated `mkdtemp` store + local dashboard only; prod
  `~/.monet/monet.db` untouched; no `mcp__monet__*` calls; all docs English.
- Files changed: `tests/test27_re21_graphdensity_dup_edges.py` (new),
  `reverse-engineering/ISSUES.md` (RE-21 confirmed), `reverse-engineering/
  dashboard.md` (RE-21 E2E note), `METRICS.md`, this diary.
