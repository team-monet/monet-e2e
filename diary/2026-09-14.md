# 2026-09-14 — run 109 (worker, sustain)

## What was done
- Confirmed `NO_NEW_RELEASE 1.11.0` (release gate script); PROD binary == npm latest ==
  `.release_handled == 1.11.0`, fully aligned (no new bump).
- Read DIRECTION.md (run-108 supervisor, `sustain`, updated 2026-09-14): E2E #1 / RE #2
  standby — the next release bump is the ONLY re-verify/flip trigger (RE-17/test24,
  RE-55/test56 candidates); coverage #3 COMPLETE (83.8% saturated). Halt list respected —
  no re-suite, no re-analysis, no re-triage, no new coverage test.
- Open issue queue EMPTY (`gh issue list --state open` → []). Git tree clean
  (git status --short empty; latest commit e1c1f6a run-108 sustain, yesterday).
- Registry tally last verified exact (run 103: 56 rows = confirmed 10 / fixed 8 /
  source 18 / removed 7 / by-design 13, 0 open) — held; no fixture/test/issue change.
- Installed `monet` = 1.11.0 (verified package.json version directly).
- No fixture/test/metric/issue change.

## Metrics
Unchanged since run 85 re-verify and runs 99-108 sustain: Tests 56, scenario 100%,
line 83.8% (saturated), XFAIL 10, XPASS 8, FAIL 0. No METRICS table row (sustain convention).

## Next steps
Only the next release bump triggers any re-verify/flip (candidates RE-17/test24,
RE-55/test56). L2 queue unchanged. test09 burst flake = sole open env item (observation only).

---

# 2026-09-14 — run 110 (worker, sustain — second invocation today)

## What was done
- Release gate: `NO_NEW_RELEASE 1.11.0`. Verified directly, not just from the script:
  node22-prefix `package.json` version = **1.11.0**, `.release_handled` = **1.11.0**,
  npm latest = **1.11.0** → installed == latest == PROD == handled, fully aligned.
  No bump → **no re-suite** (DIRECTION halt respected).
- DIRECTION.md read (run-109 supervisor, updated 2026-09-14, `sustain`): E2E #1 / RE #2
  standby, coverage #3 COMPLETE (83.8% saturated). Halt list respected — no new coverage
  test, no RE re-analysis, no upstream issue triage, no recursive re-suite.
- Integrity re-verified, both exact: `gh issue list --repo team-monet/monet-e2e --state open`
  → **EMPTY**; ISSUES.md status tally = **56 rows** (confirmed 10 / fixed 8 / source 18 /
  removed 7 / by-design 13) → **open 0**. Matches the supervisor's run-109 statement.
- Git tree clean; latest commit `7b1271c` (run 109 sustain).

## NEW state finding — the local source clone is stale (previously unrecorded)
Mechanical state check (no re-analysis, no triage):
- `~/monet/monet` HEAD = `683d261` — the #58 "hook identity" commit, i.e. the tree run 47
  reviewed as *unreleased main*. `git fetch` then `git rev-list --count HEAD..origin/main`
  = **39 commits behind**; `origin/main` = `1c7d1e5 Release 1.11.0` (== npm latest == PROD).
- `git diff --stat HEAD..origin/main -- packages/core/src packages/cli/src` =
  **140 files, +28,590 / −51,915** — includes the entire source-subsystem hard-removal
  (1.7.0) and every change through 1.11.0.
- Citation-staleness count: of the 48 distinct `*.ts` files cited across
  `reverse-engineering/*.md`, **32 have changed** in that window — including every core
  engine file (`engine.ts`, `resolution.ts`, `retrieval.ts`, `mcp-server.ts`, `storage.ts`,
  `diagnostics.ts`, `gates.ts`, `embedding-onnx.ts`, `embedding-state.ts`) and the CLI files
  (`repair-cli.ts`, `materialize-cli.ts`, `install-cli.ts`, `gate-cli.ts`, `remote-circle.ts`,
  `project-dir.ts`).
- **Why this does NOT invalidate the registry:** the behavioral rows are E2E-verified against
  the *shipped* 1.11.0 binary (XFAIL 10 / XPASS 8 / FAIL 0 still holds on 1.11.0), and many
  facts were cross-checked against the current 1.11.0 `dist` bundle rather than TS. What is
  NOT current is the **TS-line citation trail** (`file:line` anchors, "verified against main")
  for doc content written from the clone — those anchors describe a 1.6.x-era tree.
- Recorded as **GR-08** (source-read baseline must be pinned) and flagged here for the
  supervisor (DIRECTION is supervisor-owned). **The clone was NOT pulled by this run** — a
  refresh of John's clone to 1.11.0 is a John-side decision; this run only measured the delta.

## Metrics
Unchanged: Tests 56, scenario 100%, line 83.8% (saturated), XFAIL 10, XPASS 8, FAIL 0.
No METRICS row (sustain convention). Modules 26, thresholds/params 284, issues 56 (open 0).

## Next steps
- Next release bump remains the ONLY re-verify/flip trigger (candidates RE-17/test24,
  RE-55/test56; upstream #123 still OPEN as of today).
- **Supervisor item:** decide (a) refresh `~/monet/monet` to 1.11.0 and re-anchor doc
  citations, or (b) declare `dist 1.11.0` the sole RE baseline and label TS-line citations
  as historical. Until then, no doc claim should be extended from the stale TS tree.
---

# 2026-09-14 — run 111 (worker, signal-driven re-verify — **RE-57 registered**)

## What was done
- Release gate `NO_NEW_RELEASE 1.11.0`, verified directly (not just from the script): node22-prefix
  `package.json` = 1.11.0, npm latest = 1.11.0 (npm time shows no publish since 2026-09-01T10:46),
  `.release_handled` = 1.11.0 → aligned. No bump → **no re-suite** (DIRECTION halt respected).
- Read DIRECTION.md (run-109 supervisor). Its halt list (no re-suite, no coverage work, no upstream
  re-analysis/triage) was respected.
- **Signal change that DIRECTION (07:31) predates:** upstream `main` advanced past the published
  release today — `gh api repos/team-monet/monet/commits` head = `9fa38c2`
  `fix(cli): carry startupFailure into doctor --json on the failure path (#120) (#153)`, merged
  2026-09-14T02:35Z (12:35 AEST); #120 CLOSED. The released binary is still `1c7d1e5`/1.11.0, so the
  fixed behavior is absent from the shipped product and **verifiable now** (this is the pilot-prompt
  rule-4 case: a previously-blocked invariant whose relevant signal changed).
- **RE-57 registered (S3, `confirmed`, e2e_test `test57`)** — `monet doctor --json` drops
  `startupFailure` on the FAILURE path. Evidence on dist 1.11.0 (isolated temp store, public CLI):
  corrupt `monet.db` → `monet start` dies closed (rc 1) and writes `monet.db.startup-failure.json`
  (`phase=store-open`, `error.code=SQLITE_NOTADB`); the TEXT `doctor` surfaces it ("last recorded
  startup failure" + sidecar path); the SAME invocation's `--json` (`monet.recovery.v1`) returns
  `{schema,command,ok:false,dbPath,error:RepairOperationError "… (not-sqlite)",inspection:null,
  provider:{loadStatus:"not-checked"},nextCommands:[],backup:null,report:null}` with **no
  `startupFailure` key** — while the fresh/success path DOES carry `{"status":"none"}`. So the
  machine-readable surface an operator/CI triages from loses the cause; RE-50's out-of-band
  diagnosis is text-only.
- New guard `tests/test57_re57_doctor_json_startup_failure.py`, run standalone → **EXIT=2 (XFAIL)**
  (all setup assertions PASS: start fails closed, sidecar valid, text doctor surfaces it, json parses,
  `ok=false`, fresh path `none`). It asserts the fix's contract on the failure path
  (`status=="found"` + `phase` + `error.code`) so the next release flips it to XPASS.
- Files: test57 (new), `ISSUES.md` RE-57 row, `reverse-engineering/repair-cli.md` RE-57 paragraph
  (issue section, numeric order kept), `METRICS.md` row, this diary. No product source touched.
- Registry recount (programmatic, not eyeballed): **57 rows** = confirmed 11 / fixed 8 / source 18 /
  removed 7 / by-design 13 = 57, **open 0**; zero confirmed/fixed rows without a test link.
- `monet-e2e` open issues: none (`gh issue list --state open` → empty), so no new issue was needed.

## Metrics
Tests **56 → 57** (new XFAIL guard), scenario coverage 100%, line coverage unchanged **83.8%**
(no coverage work — DIRECTION #3 complete), XFAIL **10 → 11**, XPASS 8, FAIL 0.
Registry **56 → 57 rows** (confirmed 10 → 11).

## Next steps
- **Next release bump: add RE-57/test57 to the flip-check list** (alongside RE-17/test24,
  RE-55/test56). Expected after the release carrying `9fa38c2`.
- Supervisor item (carried from run 110, unchanged): refresh `~/monet/monet` to the shipped tree, or
  declare `dist <version>` the sole RE baseline (GR-08).
- test09 burst flake = sole open environment item (observation only).
