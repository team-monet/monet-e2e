# 2026-08-14 — Run 20: issue #1 fix — test01 dead assertions + run_all.py summary misreport

## What was done
1. **Open issue `team-monet/monet-e2e#1` handled** — "test01 has 2 dead
   assertions + run_all.py summary misreports test12". All three items fixed.
2. **`tests/test01_handshake.py` — two tautologies now real assertions:**
   - `clean_stdout_protocol`: was `"stray stdout" not in ""` (always True, empty
     string). `harness/mcp_client.py` now records non-JSON stdout lines in
     `self.stray_stdout_lines` (populated in the `JSONDecodeError` branch of
     `request()`, where stray protocol lines are already skipped). The
     assertion is now `not c.stray_stdout_lines` — it fails if the server ever
     leaks a banner line onto the protocol stream.
   - `server_exit_clean`: was `returncode in (0, None) or returncode is not None`
     (the second clause makes it always True). Now asserts
     `c.proc.returncode == 0`. Verified live: the server exits rc=0 on stdin
     close (no kill-fallback triggered), so the clean-exit claim is actually
     checked.
3. **`harness/run_all.py` — summary now greps `RESULT:` instead of taking the
   last stdout line.** New `_result_line()` reverse-scans for the `RESULT:`
   prefix (falls back to the last non-empty line). test12 prints
   `OBSERVED versions: [...]` *after* its `RESULT:` line, which was masking the
   real pass/fail summary — the suite now reports `RESULT: 23 passed, 0 failed`
   for test12.
4. **Full suite regression: 21/21 passing, 0% failure.** test01 still reports
   "6 passed" but all six are now falsifiable (previously 2 of the 6 were
   tautologies); the test12 summary line is corrected.

## Findings
- No product findings — this was a test-quality cleanup run. Net assertion
  count unchanged at **365**: two assertions that previously *counted* but
  verified nothing now verify real conditions, so the "assertion count =
  quality" metric (GR-03) is no longer inflated by ~0.5%.

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |

**Suite total: 21/21 passing, 365 assertions, 0% failure**

## Metric changes
- Tests 21 (unchanged), coverage 100% (unchanged), failure 0% (unchanged).
- Quality: 2 dead assertions made falsifiable (test01); `run_all.py` summary
  reporting corrected for any test that prints after its `RESULT:` line.

## Next steps
1. Sources E2E (test22 candidate, run-19-RE next step #1): source-injection
   test — create a `repo-md` source in an isolated dir, sync, assert
   chunks/concepts/published snapshot + `source_path` seal. First data-layer
   verification of the sources/sync module.
2. Scenario 9: read+write mixed-load latency distribution (oldest open item).
3. Product follow-up: re-check RE-09/RE-13/RE-14/RE-23..25 on the next
   @team-monet/monet bump.

## Meta
- Harness modified: `harness/mcp_client.py` (`stray_stdout_lines` tracking),
  `harness/run_all.py` (`_result_line`), `tests/test01_handshake.py`
  (2 assertions made real).
- Guardrails: **GR-07 added** (assertions must be falsifiable — no tautologies).
- Isolation: everything ran against the isolated `~/.monet-test` DB; prod
  `~/.monet/monet.db` and MCP server untouched; no `mcp__monet__*` calls.
  All docs English.
