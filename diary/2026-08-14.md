# 2026-08-14 — Run 20: issue #1 fix — test01 dead assertions + run_all.py summary misreport

## What was done
1. **Open issue `team-monet/monet-e2e#1` handled** — "test01 has 2 dead
   assertions + run_all.py summary misreports test12". All three items fixed.
2. **`tests/test01_handshake.py` — two tautologies now real assertions:**
   - `clean_stdout_protocol`: was `"stray stdout" not in ""` (always True, empty
     string). `harness/mcp_client.py` now records non-JSON stdout lines in
     `self.stray_stdout_lines` (populated in the `JSONDecodeError` branch of
     `request()`, where stray protocol lines are already skipped). The
     assertion is now `not c.stray_stdout_lines` — it fails if the server ever
     leaks a banner line onto the protocol stream.
   - `server_exit_clean`: was `returncode in (0, None) or returncode is not None`
     (the second clause makes it always True). Now asserts
     `c.proc.returncode == 0`. Verified live: the server exits rc=0 on stdin
     close (no kill-fallback triggered), so the clean-exit claim is actually
     checked.
3. **`harness/run_all.py` — summary now greps `RESULT:` instead of taking the
   last stdout line.** New `_result_line()` reverse-scans for the `RESULT:`
   prefix (falls back to the last non-empty line). test12 prints
   `OBSERVED versions: [...]` *after* its `RESULT:` line, which was masking the
   real pass/fail summary — the suite now reports `RESULT: 23 passed, 0 failed`
   for test12.
4. **Full suite regression: 21/21 passing, 0% failure.** test01 still reports
   "6 passed" but all six are now falsifiable (previously 2 of the 6 were
   tautologies); the test12 summary line is corrected.

## Findings
- No product findings — this was a test-quality cleanup run. Net assertion
  count unchanged at **365**: two assertions that previously *counted* but
  verified nothing now verify real conditions, so the "assertion count =
  quality" metric (GR-03) is no longer inflated by ~0.5%.

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |

**Suite total: 21/21 passing, 365 assertions, 0% failure**

## Metric changes
- Tests 21 (unchanged), coverage 100% (unchanged), failure 0% (unchanged).
- Quality: 2 dead assertions made falsifiable (test01); `run_all.py` summary
  reporting corrected for any test that prints after its `RESULT:` line.

## Next steps
1. Sources E2E (test22 candidate, run-19-RE next step #1): source-injection
   test — create a `repo-md` source in an isolated dir, sync, assert
   chunks/concepts/published snapshot + `source_path` seal. First data-layer
   verification of the sources/sync module.
2. Scenario 9: read+write mixed-load latency distribution (oldest open item).
3. Product follow-up: re-check RE-09/RE-13/RE-14/RE-23..25 on the next
   @team-monet/monet bump.

## Meta
- Harness modified: `harness/mcp_client.py` (`stray_stdout_lines` tracking),
  `harness/run_all.py` (`_result_line`), `tests/test01_handshake.py`
  (2 assertions made real).
- Guardrails: **GR-07 added** (assertions must be falsifiable — no tautologies).
- Isolation: everything ran against the isolated `~/.monet-test` DB; prod
  `~/.monet/monet.db` and MCP server untouched; no `mcp__monet__*` calls.
  All docs English.

---

# 2026-08-14 — Run 21 (reverse-engineering): gates + sync/graft from readable TS source

## What was done
1. **Pivot to the readable TS source.** `~/monet/monet-core` (`@team-monet/core`
   v0.9.0, proprietary, 155 files / ~112K LOC, 100+ unit tests) is now present,
   with real variable names + comments + types. Runs 08-10..13 reverse-engineered
   the same engine from the minified `dist` bundle. This run documents the first
   module from the TS source and cross-checks the minified findings.
2. **Priority-1 cross-check (dedup thresholds):** `src/resolution.ts` +
   `src/embedding-onnx.ts` (`MODEL_PROFILES`) CONFIRM the reverse-engineered
   `dedup-resolution.md` was exact: minified `V1`=`resolveIncoming`,
   `sT`=`createOrPair`, `pU`=`MODEL_PROFILES`, `Sd`=`LEGACY_UNMEASURED_THRESHOLDS`
   (0.72/0.50), `uU`=`DEFAULT_MODEL`="Xenova/bge-m3:cls:q8". Thresholds match:
   bge-m3:cls:q8 0.70/0.50/0.60, bge-small-en 0.78/0.50/0.70/380/0.35,
   multilingual-MiniLM 0.70/0.50. The TS source additionally documents the full
   data-derivation methodology (measure-attach-thresholds / measure-fork-and-edge-bands
   / measure-recall-floor) and the 10-value `ResolutionMode` enum + `DECIDED_RESOLUTION_MODES`
   denominator — detail the minified bundle could never show.
3. **Documented the gates/conformance/journal module** (`gates.md`): stages +
   rule_bindings + deterministic trigger-pattern matcher, the three schema CHECK
   safety boundaries (blocking-is-declaration-only, breadth-is-declaration-only,
   agent-scope-implies-tag), `gate_events`/`gate_event_stages`/`gate_meta`, the gate
   mirror (sidecar, `GATE_MIRROR_FORMAT=4`), the gate journal (`gate-journal.jsonl`,
   64MiB rotation, 2-line-per-evaluation, claim types), and the conformance "cheap half"
   (only `changed` is observable; advisory → `unavailable` backlog).
4. **Documented the sync/graft protocol** (`sync-graft.md`): `GraftPayload` v8/v15,
   `exportDelta`/`graftRows`/`batchDedup`, v8 row-convergence clock
   `(sync_revision, sync_writer)`, native-only validation, embedder-mismatch rejection.
   Reconciled the `Gf=15` unknown from `schema-migration.md` = `SYNC_PAYLOAD_PROTOCOL_VERSION`
   (payload ceiling, changelog 11→15), separate from the DB `user_version` ladder.
5. **Created the missing `reverse-engineering/ISSUES.md`** — consolidated RE-01..RE-28
   into one table (one-line summary + owning module + status open/confirmed/by-design).

## Findings
- 3 new issues: **RE-26** (`gate_events` has no retention; will become the largest
  table on busy stores), **RE-27** (conformance "cheap half" can only emit `changed`
  for blocking denies — advisory rules are permanently `unavailable`/un-retirable
  until the judgment half ships), **RE-28** (`gate_events.action_context` stores raw
  command lines verbatim — most privacy-sensitive column; local-only + scrub-covered).

## Metric changes
- Modules documented: **7 → 9** (gates + sync/graft).
- Thresholds/params identified: **78 → 110** (+32).
- Issues found: **25 → 28** (+RE-26..RE-28).
- Priority-1 (dedup threshold location) re-confirmed against readable source with zero
  corrections needed — the 08-10 minified-bundle reverse-engineering was exact.

## Next steps
1. Continue the readable-TS pass over the remaining undocumented engine.ts subsystems:
   temporal/usefulness-decay/arousal + V-A ranking (`stats-ranking.test.ts`,
   `va-ranking-probe.test.ts`), lifecycle-edges (`lifecycle-edges.ts`), statement-trace,
   skeleton-mirror, and the budget system (embed/write/query-budget).
2. Cross-check the other minified-bundle docs (search/migration/circle/contradiction)
   against their readable TS counterparts for any drift.
3. Re-check RE-09/RE-13/RE-14/RE-23..28 on the next @team-monet/monet bump.

## Meta
- Guardrails honored: source read-only; prod `~/.monet/monet.db` untouched; no
  `mcp__monet__*` calls; proprietary-source detail kept to behavior/params/issue
  summaries (no bulk TS quoting); table pipes escaped where needed; all docs English.

# 2026-08-14 — Run 23: RE-19 (workstream merge hard-delete) → FIXED in 1.6.1 (first XPASS)

## What was done
1. **RE-issue verification loop** (priority per pilot prompt): scanned
   `reverse-engineering/ISSUES.md` for `open` + behaviorally-verifiable issues.
   Selected **RE-19** (S2 — `mergeCircle` HARD-DELETES workstream concepts,
   counted `noop`), the highest-severity open behavioral issue with a clean
   MCP-surface reproduction.
2. **Wrote `tests/test23_re19_workstream_merge.py`** (XFAIL contract): open a
   workstream (2 open items) + a normal concept in circle A, `merge` A→B, then
   assert the workstream survives (moved, not hard-deleted) with open items
   intact. Exit 2 = bug present, 3 = fixed.
3. **Result: XPASS (exit 3)** — workstream concept moved to B (`action:'moved'`,
   `kind='workstream'`), `memory_workstreams` still lists it active, the 2 open
   items survive byte-intact in the concept body JSON.
4. **Source cross-check** in readable `~/monet/monet-core/src/engine.ts`
   `mergeCircle()`: the per-concept loop now does `if (row.kind === "workstream")
   continue;`, then `moveCircleScopedTables` relocates all workstreams (reporting
   `moved`, or `merged` into an existing destination workstream via
   `workstreamMerges`). This is a real behavior change vs the v1.5.2
   `hardDeleteNativeConcept` the RE-14 doc recorded.
5. **Updated ISSUES.md**: RE-19 → `fixed`, `e2e_test=test23`; added a `fixed`
   status to the legend (first exercise of the XPASS→fixed flow).
6. **Updated circle-routing.md**: mergeCircle section + RE-19 entry now document
   the 1.6.1 semantics (workstream relocation + the `counts.error=0` note is now
   by-design under the atomic `immediateTransaction`).

## Findings
- **RE-19 FIXED in 1.6.1** (primary data-loss arm). Residual `counts.error`
  hardcoded 0 is now by-design: the whole merge runs under one
  `immediateTransaction` that rolls back atomically on any item failure, so a
  per-item error count is moot. No new issue warranted.
- This is the first XPASS in the suite — the XFAIL/XPASS tracking mechanism
  (run 22) proved out end-to-end: a known-bug test flipping green correctly
  surfaced a real upstream fix.

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |
| test22 (RE-07) | XFAIL (bug still present — expected) |
| test23 (RE-19) | XPASS (bug appears fixed) |

**Suite total: 21/23 passing, 1 xfail, 1 xpass, 372 assertions, 0% failure**

## Metric changes
- Tests 22 → 23, coverage 100% (unchanged), failure 0% (unchanged).
- Assertions 367 → 372 (+5 setup checks in test23).
- RE-19 status open → fixed (first XPASS-driven issue closure).

## Next steps
1. Convert the next open behavioral issues into XFAIL tests, in severity order:
   RE-17 (S3, store into archived circle), RE-26 (S2, gate_events no retention —
   needs a busy-store approach), RE-21 (S4, graphDensity includes
   possible_duplicate_of — dashboard-API testable), RE-04 (S3, Latin-only
   lexical arm), RE-05 (S4, source concepts skip floor).
2. Sources E2E (source-injection test) — the last major undocumented-at-E2E-level
   module (sources/sync); RE-23/RE-24/RE-25 live there.
3. Re-check remaining `open`/`confirmed` issues (RE-04/05/07/09/10/11/17/21/23/
   24/26) on the next @team-monet/monet bump.

## Meta
- Guardrails honored: isolated `~/.monet-test` only (fresh timestamped circles),
  prod `~/.monet/monet.db` untouched, no `mcp__monet__*` calls, all docs English.
- Files changed: `tests/test23_re19_workstream_merge.py` (new),
  `reverse-engineering/ISSUES.md` (RE-19 fixed + legend), `reverse-engineering/
  circle-routing.md` (merge semantics), `METRICS.md`, this diary.

---

# 2026-08-14 — Run 24: RE-17 (archived-circle store guard) → CONFIRMED

## What was done
1. **RE-issue verification loop** (priority per pilot prompt): scanned
   `reverse-engineering/ISSUES.md` for `open` + behaviorally-verifiable issues.
   Selected **RE-17** (S3 — `storeInternal` has no archived-circle guard), the
   next highest-severity open behavioral issue with a clean MCP-surface
   reproduction (RE-26 is S2 but needs a busy-store approach).
2. **Wrote `tests/test24_re17_archived_circle_store.py`** (XFAIL contract): store
   an anchor concept in a fresh circle, `archive` it via `memory_circle_manage`,
   then assert the DESIRED contract — a `memory_store` into the archived circle
   is REFUSED (archive = "nobody works here", mirroring the retired-concept guard
   and reassignCircle's `assertArchivedCircleMoveAllowed` door).
3. **Result: XFAIL (exit 2)** — the bug is present: `memory_store` into the
   archived circle returned `action='ambiguous'` with a live `conceptId`, and the
   concept landed in the hidden circle (DB `concepts.circle` = the archived name).
4. **Engine cross-check** in readable `~/monet/monet-core/src/engine.ts`:
   `storeInternal` enforces only the `*` breadth-marker guard (line 4255) — no
   archived check anywhere on the store path. The engine's `isArchivedCircle`
   helper (line 4113) exists but is consulted ONLY by
   `assertArchivedCircleMoveAllowed` (line 4146, reassignCircle's
   archived-destination door for circle-local blocking rules). RE-17 holds.
5. **Updated ISSUES.md** (RE-17 → `confirmed`, `e2e_test=test24`) and
   **circle-routing.md** (RE-17 entry now records the E2E confirmation + the
   `isArchivedCircle`-is-move-only cross-check).

## Findings
- **RE-17 CONFIRMED (S3)** — archive hides recall, not writes. A caller that
  archives a circle and later stores into it gets a normal success and the
  concept vanishes from store-wide recall until an explicit unarchive. The
  correct behavior (refuse the write) is what test24 asserts; it stays XFAIL
  until the product adds the guard.

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |
| test22 (RE-07) | XFAIL (bug still present — expected) |
| test23 (RE-19) | XPASS (bug appears fixed) |
| test24 (RE-17) | XFAIL (bug still present — expected) |

**Suite total: 21/24 passing, 2 xfail, 1 xpass, 376 assertions, 0% failure**

## Metric changes
- Tests 23 → 24, coverage 100% (unchanged), failure 0% (unchanged).
- Assertions 372 → 376 (+4 setup checks in test24).
- RE-17 status open → confirmed (E2E reproduction pinned).

## Next steps
1. Continue the open-behavioral-issue XFAIL conversion, in severity order:
   RE-26 (S2, gate_events no retention — busy-store approach), RE-21 (S4,
   graphDensity includes possible_duplicate_of — dashboard-API testable), RE-04
   (S3, Latin-only lexical arm), RE-05 (S4, source concepts skip floor), RE-09
   (S3, supportedSchemaVersion hardcoded), RE-24 (S2, updateSource access
   mutation).
2. Sources E2E (source-injection test) — the last major undocumented-at-E2E-level
   module (sources/sync); RE-23/RE-24/RE-25 live there.
3. Re-check remaining `open`/`confirmed` issues (RE-04/05/07/09/10/11/17/21/23/
   24/26) on the next @team-monet/monet bump.

## Meta
- Guardrails honored: isolated `~/.monet-test` only (fresh timestamped circle),
  prod `~/.monet/monet.db` untouched, no `mcp__monet__*` calls, all docs English.
- Files changed: `tests/test24_re17_archived_circle_store.py` (new),
  `reverse-engineering/ISSUES.md` (RE-17 confirmed), `reverse-engineering/
  circle-routing.md` (RE-17 E2E note), `METRICS.md`, this diary.

---

# 2026-08-14 — Run 25: sources/sync E2E (test25) — RE-29 (isolation gap) + RE-30 (macOS EACCES) confirmed

## What was done
1. Picked the longest-open next step (runs 22–24): the sources/sync module — the
   last subsystem never exercised at E2E level. Registered a repo-md source and
   drove the 4 source MCP tools end-to-end in an isolated store.
2. Built the first source-injection E2E, `tests/test25_sources_e2e.py`:
   - creates a real git worktree of Markdown in a fresh `mkdtemp` base,
   - registers a repo-md source via `monet source add` (server identity
     `MONET_CALLER_ID`/`MONET_PROJECT_ID`),
   - drives `source_list`/`source_status`/`source_sync`/`source_path` over MCP,
   - asserts the DESIRED contract: `source_sync` publishes a snapshot
     (filesIndexed>0, `source_path` returns a sealed path). Exit 2 (XFAIL) while
     the bug is present.
3. **RE-30 CONFIRMED (S2)**: `source_sync` fails `EACCES: permission denied,
   rename` on macOS, 4/4 reproducible (across /tmp and $TMPDIR, and with HOME
   redirected — location-independent). Root cause in `source-materializer.ts`:
   `sealSnapshot()` chmods the staged tree to `0o500`, then
   `renameSync(tree, snapshotPath)` fails because APFS refuses to rename a
   non-writable directory even in-place (same parent). The code's own
   "stage beside the final variant" mitigation (line 2225) is insufficient on
   macOS 15.x. The whole source_sync feature is non-functional via MCP on macOS.
4. **RE-29 CONFIRMED (S2)**: `sourceStorageDir` = `resolve(homedir(), ".monet",
   "sources")` and is NOT scoped by `-d` — `-d /tmp/<isolated>` isolates the DB
   but source snapshots/locks still land in prod `~/.monet/sources`. Verified
   live during the probe (source materialized under `~/.monet/sources/repo-md/
   <id>/` despite `-d`). Harness workaround: redirect `HOME` to a temp dir for
   both the CLI and the MCP server (test25 asserts this with
   `source_storage_isolated_via_home`).

## Findings
- Two new issues (RE-29, RE-30), both S2. RE-30 is the first macOS-platform
  hard-failure found by the suite; RE-29 is an isolation footgun that would
  have let the E2E harness silently write to prod.
- Source surface otherwise behaves as documented: registration, `source_list`
  (env-gated), `source_status` (pending-initial-sync → failed + lastError),
  `source_path` (correctly refuses "no published snapshot" after a failed sync).

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |
| test22 (RE-07) | XFAIL (bug still present) |
| test23 (RE-19) | XPASS (fixed) |
| test24 (RE-17) | XFAIL (bug still present) |
| test25 (RE-30) | XFAIL (bug still present) |

**Suite total: 21/25 passing, 3 xfail, 1 xpass, 381 assertions, 0% failure**

## Metric changes
- Tests 24 → 25, coverage 100% (unchanged), failure 0% (unchanged).
- Assertions 376 → 381 (+5 setup checks in test25).
- RE-29 → confirmed, RE-30 → confirmed (e2e_test=test25).

## Next steps
1. Re-check RE-30 on the next @team-monet/monet bump (the sealed-dir rename fix
   is a one-liner candidate: chmod the tree writable before rename, or rename
   then seal).
2. Convert the next open behavioral issue: RE-26 (S2, gate_events no retention
   — busy-store approach), RE-21 (S4, graphDensity possible_duplicate_of —
   dashboard API), RE-04 (S3, Latin-only lexical arm), RE-05 (S4, source
   concepts skip floor — now reachable via test25's source scaffolding).
3. Re-check remaining open/confirmed issues on the next version bump.

## Meta
- Guardrails honored: all source work in a fresh mkdtemp store; `HOME` redirected
  so nothing touched prod `~/.monet/sources` (verified absent after the run);
  prod `~/.monet/monet.db` untouched (schema 12, knowledge_sources 0 rows,
  unchanged); no `mcp__monet__*` calls; all docs English.
- Probe temp dirs (`/tmp/monet-src-probe`, `/var/folders/.../monet-src-e2e-*`)
  and the accidental `~/.monet/sources` created during the probe were cleaned up.
- Files changed: `tests/test25_sources_e2e.py` (new), `reverse-engineering/
  ISSUES.md` (RE-29/RE-30 + isolation note), `reverse-engineering/
  sources-sync.md` (RE-29/RE-30 detail + E2E note), `METRICS.md`, this diary.

---

# 2026-08-14 — Run 26: RE-23 refined + test26 (source identity discoverability)

## What was done
1. **RE-issue verification loop** (priority per pilot prompt): scanned
   `reverse-engineering/ISSUES.md` for `open` + behaviorally-verifiable issues.
   Selected **RE-23** (S3, source tools env-gated), the next open behavioral
   issue with a clean MCP-surface reproduction.
2. **Probed the installed 1.6.1 source surface with the identity env vars
   unset** and found the run-19 reverse-engineering was INCOMPLETE. Run 19 read
   only the core (`dist/index.js`) `deriveOptsFromEnv`, which omits
   `sourceAuthorizationContext` when MONET_CALLER_ID/MONET_PROJECT_ID are unset.
   But the CLI `start` command pre-populates the identity BEFORE the server
   reads it:
   - `MONET_CALLER_ID  = deriveCallerId()`  → env, else `"local-agent"`
   - `MONET_PROJECT_ID = deriveProjectId()` → env, else git-origin URL, else
     `"<basename>-<sha8>"`
   so `sourceAuthorizationContext` is ALWAYS present and the run-19
   "trusted source authorization context is unavailable" throw is unreachable
   via `monet start`.
3. **Measured the REAL behavior** (probe, then E2E test26): with a source
   registered under `e2e-caller`, a server started with the identity unset
   returns `source_list -> {"sources": []}` (SILENT empty) and
   `source_status`/`source_path`/`source_sync` → `"source is unavailable"`
   (non-disclosing by design — the engine comment says denied and removed ids
   share one message).
4. **Wrote `tests/test26_re23_source_identity_discoverability.py`** (XFAIL
   contract): register a repo-md source under `e2e-caller`, CONTROL (with
   identity) confirms `source_list` returns 1 source, then strip the identity
   and assert the DESIRED contract — the mismatch must be discoverable, not a
   bare `[]`. XFAIL (exit 2), 4 setup checks green.
5. **Updated RE-23** → `confirmed` (e2e_test=test26) with the refined summary
   in ISSUES.md, and rewrote the RE-23 entry in sources-sync.md.

## Findings
- **RE-23 REFINED (S3).** The original "opaque context-unavailable error" never
  fires on the `start` path. The real defect is a SILENT empty `source_list`
  under a fallback identity: a user who registers sources under a specific
  caller but forgets to export `MONET_CALLER_ID`/`MONET_PROJECT_ID` gets a clean
  `[]` with no hint that sources exist but are identity-hidden — arguably worse
  than an opaque error because it looks like a healthy "no sources" state.
- `source_status`/`source_path`/`source_sync`'s `"source is unavailable"` is
  by-design non-disclosing (denied and removed ids share one message), so it is
  NOT part of the test contract; only the silent `source_list` is asserted.
- **test09 concurrency flaked once** under full-suite load (burst 15/20, one
  burst server died with BrokenPipeError) — this is the KNOWN model-load memory
  contention documented in the server-ops skill. Re-ran standalone: 15/15 green;
  clean full-suite re-run: green. Not a product regression.

## Run summary
| Test | Result |
|------|--------|
| test01..test21 | all PASS (regression) |
| test22 (RE-07) | XFAIL |
| test23 (RE-19) | XPASS (fixed) |
| test24 (RE-17) | XFAIL |
| test25 (RE-30) | XFAIL |
| test26 (RE-23) | XFAIL (new) |

**Suite total: 21/26 passing, 4 xfail, 1 xpass, 385 assertions, 0% failure**

## Metric changes
- Tests 25 → 26, coverage 100% (unchanged), failure 0% (unchanged).
- Assertions 381 → 385 (+4 setup checks in test26).
- RE-23 status open → confirmed (refined; e2e_test=test26).

## Next steps
1. Re-check RE-30 (macOS EACCES) and RE-23 on the next @team-monet/monet bump.
2. Convert the next open behavioral issue, in severity order: RE-26 (S2,
   gate_events no retention — busy-store approach), RE-04 (S3, Latin-only
   lexical arm), RE-05 (S4, source concepts skip floor — now reachable via
   test25/test26 source scaffolding), RE-21 (S4, graphDensity
   possible_duplicate_of — dashboard API).
3. Re-check remaining open/confirmed issues (RE-04/05/07/09/10/11/17/21/23/
   24/26) on the next version bump.

## Meta
- Guardrails honored: isolated mkdtemp store + HOME redirect (RE-29) so nothing
  touched prod `~/.monet/sources` or `~/.monet/monet.db` (verified via
  `source_storage_isolated_via_home`); no `mcp__monet__*` calls; all docs
  English.
- Files changed: `tests/test26_re23_source_identity_discoverability.py` (new),
  `reverse-engineering/ISSUES.md` (RE-23 refined + confirmed),
  `reverse-engineering/sources-sync.md` (RE-23 refinement + start
  identity-derivation note), `METRICS.md`, this diary.
