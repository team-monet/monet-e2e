# Monet production E2E — run 126 (2026-09-22, worker)

## Headline

`NO_NEW_RELEASE 1.11.0` → no release-shaped work, so the run collected the wake signal run 125 had
written for itself: **promote the storage-resolution probes into the suite**. Both probes are now
suite-linked guards, scenario 12 has a home, and the first attempt at the bare-call guard class
exposed a GR-01 hole in our own harness — now closed as **GR-10**. Suite: **64 tests = 39 PASS +
17 XFAIL + 8 XPASS + 0 STALE + 0 FAIL** (exit 0), i.e. run 125's 61-test tally +1 PASS +2 XFAIL.

## Gate (read at run time, G-5)

- Pre-run script: `NO_NEW_RELEASE 1.11.0`. Verified directly, not carried: installed
  `.../@team-monet/monet/package.json` = **1.11.0** = npm `latest` → no bump, no pivot.
- Clone: `HEAD` = `origin/main` = **`d920bc2`** "fix(core): refuse to open a store newer than this
  build's schema (#107) (#155)", status clean (`## main...origin/main`).
- Upstream read at run time: **#162 `OPEN`, 0 comments, `updatedAt 2026-09-21T06:11Z`** (no reply
  yet — the RE-62 flip condition has not fired). Open PRs: **159** (draft, `caa9d88`, the #158/#156
  ceiling work) and **152** (dependabot).

## Objective

Turn probe-only **RE-62** / **RE-63** (run 125) into re-run-safe suite tests, and give **scenario 12
(storage resolution)** a permanent home — verifying the real user path in the isolated workspace,
**product source untouched** (tests + harness only, throwaway stores, sandboxed `HOME`).

## Scenario 12 — `tests/test63_storage_resolution.py` (positive, PASS)

"Which store does a bare `monet` call serve?" — every entry point must agree on ONE file.

- **Result: PASS — 44 checks, 0 failed, 27.5 s**, on the first suite run.
- Shaped arms: default `$HOME/.monet`; `MONET_STORAGE_DIR` absolute; project rung via
  `MONET_PROJECT_DIR`; `CLAUDE_PROJECT_DIR` beaten by `MONET_PROJECT_DIR`; cwd rung; explicit
  `-d`; `MONET_STORAGE_DIR` relative (display arm). Plus a landing/persistence journey: store
  through a bare server, then re-read the same row through a *different* surface.
- Harness work this needed (isolated repo, not product): `MonetClient(data_dir=None)` now spawns the
  **bare** `monet start` (no `-d`) — the actual user path — and `run_cli(..., cwd=)` gained a cwd arm.

## RE-62 guard — `tests/test64_re62_storage_rung_disclosure.py` (XFAIL, exit 2)

`REPRO 16/16 ok, DESIRED 0/4 met, 8.0 s`. Two legs, both re-run safe (fresh sandbox per leg):

- **H1 — the subdirectory blind spot, with NO flip.** `MONET_PROJECT_DIR=<proj>`, cwd `<proj>`: the
  bare server stores the token into **`<proj>/.monet`** (concepts 1). A bare `status` at
  **`<proj>/sub`** and at **`<proj>/sub/deep`** reports **`$HOME/.monet` (concepts 0)**, and its
  `memory_search` returns **0 hits** for a row that is physically in the project store. The rung
  needs the directory to exist *and* the cwd to be exactly it (no upward search), and that
  subdirectory call **creates `$HOME/.monet/monet.db`**.
- **H2 — the silent flip.** Before: bare call in `<proj>` → `$HOME/.monet`, hit **1**. Then one
  documented flag (`monet start -d <proj>/.monet`) creates the rung. After: store =
  **`<proj>/.monet`** ≠ the store holding the row → hit **0**, while the row still EXISTS in the old
  store (`observation_tokens` = 1). Nothing is lost; it becomes unreachable — permanently, silently.
- DESIRED (unmet, 4): disclose a project-local rung choice; search upward (or notice the divergence);
  `status` from a subdirectory shows the store the agent host is writing; the diagnosis surface
  (`doctor --dir ~/.monet`) does not answer about a store that is not in use.

## RE-63 guard — `tests/test65_re63_relative_storage_display.py` (XFAIL, exit 2)

`REPRO 4/4 ok, DESIRED 0/2 met, 1.5 s`. A relative `MONET_STORAGE_DIR` is printed **verbatim** by
`status` stdout and by the bare `start` stderr, while `status` stderr and `doctor --json .dbPath`
print the resolved absolute path. New this run: the harm is now *named* — a reader resolving the
printed string against its **own** cwd lands on `/Users/codalee/.monet-test/rel-store/monet.db`, a
different file from the served `<proj>/.monet/monet.db`. The stronger ask is in #162: resolve
`MONET_STORAGE_DIR` into the reported string on every surface.

## GR-01 incident and GR-10 (recorded because it is evidence about the process)

`test64`'s **first** run left 4 bare `monet status` calls **outside** the env wrapper, so they
resolved the REAL `$HOME/.monet` and opened the prod store — exactly what GR-01 forbids. Measured
aftermath, not assumed:

| Artifact | Reading |
|---|---|
| prod main DB (`~/.monet/monet.db`) | sha256 `8acefcc2…`, 82,239,488 B, mtime **2026-09-21 22:11:15 — UNCHANGED** |
| `-wal` | 8,178,232 B, mtime **2026-09-22 08:06:51** (touched; the live prod MCP server also writes — coincidence recorded, not claimed as causation) |
| prod health after | `doctor --json`: integrity `ok`, schema `13`, migration `none`, pin `Xenova/bge-m3:cls:q8` (`source: migrated`), assessment `unknown` (expected without `--check-provider`) |
| prod content | `status` 408 concepts / 725 observations / 11 workstreams; raw rows 421 / 727; `user_version` 13 |

No `memory_store`/`memory_search` was ever issued against prod; only `status` (read path) ran there.
**GR-10 registered** (`GUARDRAILS.md`): a test that must call the CLI with NO `-d` MUST sandbox
`HOME` + `MONET_PROJECT_DIR`/`CLAUDE_PROJECT_DIR`/`MONET_STORAGE_DIR`/`MONET_CIRCLE`/`MONET_CALLER_ID`
AND hard-assert that every surface-reported path is inside that sandbox *before* the scenario is
judged; an out-of-sandbox path aborts the test loudly. `-d` is no defence for this test class.
Implemented as `sandboxed()` in both new guards (`test63` uses it on every arm).

## Suite (GR-09 — measured this run, never carried forward)

```
SUITE: 39/64 passed, 17 xfail (known open bugs — expected), 8 XPASS (bug appears fixed)
```
New lines: `test63` PASS 44/0 (27.5 s) · `test64` XFAIL REPRO 16/16, DESIRED 0/4 (8.0 s) ·
`test65` XFAIL REPRO 4/4, DESIRED 0/2 (1.5 s). 0 FAIL, 0 STALE, no drift; the 8 XPASS are the
standing set (test23/27/30/33/51/52/53/54).

## Result

- RE-62, RE-63 → **confirmed**, now with suite links (`ISSUES.md`); the run-125 `/tmp/r125_*`
  probe-only dependency is replaced by committed tests.
- Scenario 12 exists, is green, and pins the six-surface agreement the 1.11.0 fix delivered.
- New guardrail GR-10 closes a hole in our own harness discipline (prod-store exposure found by
  accident, bounded and verified).
- No product code touched anywhere in this run.

## Next wake signal

Release bump → re-run `test63/64/65` (a documented rung, an upward search, a resolved display, or a
schema/public-surface change moves these tables) · **#162 comment or PR → flip test64/test65** (0
comments as of this run) · standing: #161 handshake (test62 XFAIL), the #155 ceiling fix reaching a
release (test58 XFAIL → XPASS), PR #159 (draft) / #160, #117 (test61).

## Records

`METRICS.md` (run 125 backfilled — its row was never written — plus run 126), `GUARDRAILS.md`
(GR-10 + change history), `reverse-engineering/ISSUES.md` (RE-62/RE-63 → confirmed + suite links),
`reverse-engineering/storage.md` (run-126 section: what the guards add over the probes).
Issue: https://github.com/team-monet/monet/issues/162
