# 2026-08-12 — Run 11: test15 added (contradiction DISMISS decision path) + test09 flake diagnosed & hardened

## What was done
1. **New test15_dismiss_decision_path.py** (scenario 6 extension) — the open
   question from run 9 (next step #2): "what state does the `dismiss`
   decision leave (body/flag/revisions)?" Runs the same 3-obs + correction
   fixture TWICE on fresh circles — once resolved `accept-new` with body,
   once `dismiss` — and pins down the data-layer delta between the two
   verdicts.
2. **test09 flake observed & hardened.** The first full-suite run failed
   test09's burst phase (`burstB ok=15/20`, `unique=16`, then a
   BrokenPipeError on the post-burst read loop — a burst client's server
   process died). Standalone test09 passed 3/3 before and 3/3 after, and two
   subsequent full-suite runs passed 15/15 — so it is a resource-contention
   flake, not a product regression. The test now records per-writer
   exceptions and surfaces non-banner server stderr so any future
   recurrence is diagnosable instead of silently masked.
3. Full suite regression: 15/15 passing, 176 assertions (160 → 176), 0%
   failure.

## Run summary
| Test | Scenario | Result | Notes |
|------|----------|--------|-------|
| test01..test14 | 1..14 | all PASS | regression (from run 9) |
| test15_dismiss_decision_path.py | 6 ext. | **16/16 PASS new** | dismiss vs accept-new verdict delta |

**Suite total: 15/15 passing, 176 assertions (160 → 176), 0% failure**

## Finding 14 — `dismiss` is a pure no-verdict close: no body, no revision, no observation
### 14a. dismiss works WITHOUT `body` even on a multi-observation concept
Contrast with run 9's finding 13b: `accept-new` on a 2+ prior-observation
concept is REFUSED without a reconciled body (anti-guess guard — superseding
an arbitrary prior would be a guess). `dismiss` on the SAME fixture succeeds
body-less: it supersedes nothing, so no guess is being made. The guard is
verdict-specific, not contradiction-wide.

### 14b. dismiss closes the contradiction
`memory_overview`: openContradictions empty, `counts.disputed` = 0.
Data layer: the contradictions row is RETAINED with `status='dismissed'`
(terminal), `resolved_by='monet-e2e-test'`, `resolved_at` populated, `kind`
kept ('value-conflict'). No row deletion.

### 14c. dismiss leaves body/observations/revisions untouched
- Concept body unchanged (observation appends from store time remain — the
  CORRECTION observation text is legitimately in the body because stores
  append; what dismiss must NOT do is inject a verdict text).
- obsCount unchanged (no new observation row).
- **concept_revisions: ZERO rows for dismiss** vs exactly 1 for
  accept-new-with-body on the same fixture. Revisions record body-changing
  ops only; dismiss changes no body.
- needsSynthesis stays True (the correction observation re-armed it; resolve
  is not a synthesize — consistent with run 9 finding 13).

### 14d. verdict matrix now complete (accept-new vs dismiss)
| Aspect | accept-new (w/ body) | dismiss |
|--------|---------------------|---------|
| body required on 2+ obs | YES (refused otherwise) | NO |
| concept body | replaced with reconciled body | unchanged |
| concept_revisions row | +1 | +0 |
| observations | unchanged | unchanged |
| contradiction row | retained, terminal status | retained, `status='dismissed'` |
| needsSynthesis | untouched (stays True) | untouched (stays True) |
| disputed count | 0 after | 0 after |

## Finding 15 — test09 burst flake: resource contention, now instrumented
First full-suite run: burstB had 5/20 stores fail (writer exceptions) and a
subsequent search hit BrokenPipeError — one burst server process died mid-
burst. Not reproducible standalone (3/3) nor in 2 later full-suite runs.
Probable cause: 4 concurrent `monet start` processes each load the bge-m3 q8
embedder; under the accumulated load of a fresh suite run one process died.
Product risk low (single process death under 4x model load; no lock errors
recorded on survivors), but the test previously SILENTLY swallowed writer
exceptions — that is now fixed: per-writer exception text and unusual server
stderr are printed as INFO when present. If the flake recurs, the next run
will show exactly which exception/error killed it.

## Metric changes
Tests 14 → 15, coverage 100% maintained (matrix 1..10 all covered; scenario 6
extended with the dismiss path), assertions 160 → 176, failure rate 0%.

## Next steps
1. Scenario 9: read+write mixed-load latency distribution (still open; the
   burst flake may deserve a 3rd burst sub-phase with a retry/backoff study).
2. Contradiction: `dismiss` with a `body` — does the handler accept it and
   record anything (e.g. detail) or ignore it? (boundary probe)
3. Scenario 6 x 10: contradiction on a SYNTHESIZED concept then dismiss —
   does dismiss interact with needsSynthesis differently than accept-new?
4. Product follow-up (reverse-engineering agent owns): confirm dismiss
   handler source (status value 'dismissed' vs 'resolved' — is the status
   string version-dependent?); RE-13 candidate if the two verdicts write
   different status strings without a shared enum.

## Meta
- Harness additions: `tests/test15_dismiss_decision_path.py`; modified:
  `tests/test09_concurrency.py` (writer-exception recording + stderr
  surfacing — no assertion changes).
- Production `~/.monet/monet.db` and MCP server untouched — everything ran in
  `-d` isolated dirs (`monet doctor -d ~/.monet-test --check-provider` first:
  schema 12, integrity ok, multilingual pin).
- Guardrails observed: no mcp__monet__* calls, no prod DB access, push to
  monet-e2e repo only, all docs English.
