# 2026-09-18 — run 118 (worker, suite re-run + STALE class + RE-58 hardened to the measured flip constraints)

Run source: cron, scheduled 08:00 AEST, actual start 08:00:21 AEST (dispatch on time).

## What was done

- Release gate `NO_NEW_RELEASE 1.11.0`, verified directly rather than from the script alone:
  installed `/Users/codalee/.local/share/monet-node22/lib/node_modules/@team-monet/monet`
  = **1.11.0**, npm `latest` = **1.11.0**, `.release_handled` = **1.11.0**, served
  `dist/cli.js` sha256 `0c579e23…` (unchanged since run 116) → **no bump**. GitHub side:
  newest tag `v1.11.0`, latest release `v1.7.1`, `main` HEAD `9fa38c2`.
  → There is nothing to re-verify about a release; the run pivoted to the thing nobody had
  measured recently — **the suite itself**.
- Read `DIRECTION.md` + `supervisor-state.md`, then obeyed
  `monet-e2e/pilot-cron-prompt.md` as required at the start of every run.

## Signal 1 — the suite was NOT green; it had not been run

Runs 114/116/117 each recorded "no bump → no re-suite" and carried the tally
(`Tests 58, XFAIL 12, XPASS 8, FAIL 0`) forward. This run executed the suite anyway:

```
SUITE: 34/58 passed, 12 xfail, 8 XPASS, 4 STALE (harness drifted — re-baseline)
SUITE_EXIT=0
```

Four tests that the carried-forward tally called passing were in fact **failing**. Root cause
per test, each reproduced standalone (product code untouched, GR-01 respected):

| test | measured failure | cause |
|------|------------------|-------|
| test42_conformance | `setup_conformance.ts_exists` FAIL → exit 1 before any product code runs | pinned target `packages/core/src/conformance.ts` was **deleted upstream** in `f20756b` ("Rebuild gate measurement around the governed moment (#60)", 2026-08-21) |
| test43_db_index | `build_rc0` FAIL (esbuild) | `cli/src/db/index.ts` now imports `MOMENT_SPOOL_FILENAME` / `startupFailurePath` from `@team-monet/core` and no longer exports `getGateMirrorPath` / `getGateJournalPath`, which the driver imports — the driver's stub no longer compiles against `main` `9fa38c2` |
| test46_retrieval | `build_rc0` FAIL (esbuild) | same class: pure-source driver vs. the 1.11.0-era source tree |
| test49_resolution_dedup | driver RAN (43/46 checks) and 3 checks disagreed: `corr_mode` (`ambiguous-fork` vs pinned `correction-attach`), `corr_attachTo`, `corr_no_dup_edge` | expectation **predates the RE-47 fix** — ambiguous-band correction forks, which is exactly why **test53 XPASSed in this same run** |

**Drift window:** the monorepo moved to `9fa38c2` at the 2026-09-15 09:18 pull; the last
successful driver builds are the Sep-2 bundles; 09-14..09-17 recorded no re-suite. So the
breakage is ≥3 days old and every standby run since reported green — **a tally nothing had
executed**. That is not a measurement, it is a belief.

Two harness facts worth keeping: test42's target module no longer exists at all (a deleted
module cannot be covered by re-baselining the driver — it needs a *new* subject), and
test43/46 fell through to the **leftover Sep-2 bundle** after a failed build, which can still
print PASS: a false green sitting behind a real failure.

## Fix shipped this run — a result CLASS for harness drift, not a driver repair

- `harness/run_all.py`: new exit code **5 = STALE**. Rationale: the suite must not go red for
  harness drift (a red suite hides real regressions) and must not go green either (a silent
  green hides missing coverage). STALE keeps the suite exit 0, prints a loud block naming the
  files, and states that their RESULT line is **not** evidence about the product.
- test42/43/46/49 now return 5 with the specific drift cause; test43/46 **stop before running a
  stale bundle** (no fall-through false green).
- `GUARDRAILS.md` **GR-09**: every run must measure the suite it reports; never carry a tally
  forward; a test whose pinned target is gone is STALE, not PASS/FAIL.

## Signal 2 — RE-58 guard hardened to the flip constraints run 117 measured

Run 117 pre-registered three constraints on the flip signal; all three are now discharged in
`tests/test58_re58_schema_ceiling_shipped.py`:

1. **Four arms instead of two** — bare header, real seeded header, **orphan `-wal` without
   `-shm`**, **hot `-journal`**. The stray shapes are the #155 fix's *least-verified* path
   (no committed test in the PR can make the preflight return `null`), i.e. the only coverage
   that path will ever have.
2. **One testable `verdict()` helper** — `xpass` / `xfail` / `inconclusive`, 8 offline
   self-checks, so a passing guard cannot be the product of a weak assertion.
3. **Lock contention = INCONCLUSIVE, never XFAIL** — exit **4** on a contended probe.

Measured on dist 1.11.0 (isolated `-d` stores): **all four arms are accepted and served.**
Each store is bootstrapped in place (27 `CREATE TABLE`s) after a bare `user_version=14` stamp,
`user_version` stays 14, `memory_store` → `memory_search` returns the row (a second independent
session for the seeded arm), and the hot `-journal` store is WAL-converted. The same store
still makes **`monet repair` REFUSE** ("newer than supported schema 13") — the doctor/repair
vs. start asymmetry stands. **RE-58 verdict unchanged: XFAIL** (exit 2), flip signal remains
the refusal only (#156's circle-map write before the refusal is why the guard does not assert a
write-free store).

## Upstream state (G-5 — read directly, never carried forward)

- PR **#155** (schema-version ceiling): `state=OPEN`, `mergedAt=null`, HEAD `770cd98`, base
  `main`, `mergeStateStatus=CLEAN` — unchanged since run 117, merge gate still withheld.
- `main` HEAD `9fa38c2` (2026-09-14, #153); newest tag `v1.11.0`; npm latest 1.11.0.
- No new release → the ceiling is in no published artifact → no flip is possible yet.

## Unchanged

Tests 58 (no test added or removed — 4 of them changed *class* to STALE), XFAIL 12, XPASS 8,
FAIL **0** (now measured rather than assumed), scenario coverage 100%, line coverage 83.8%
(the 4 STALE routes contribute none until re-baselined).

## Follow-ups for the next run

- Re-baseline test43/46/49 against `9fa38c2` (drivers + cores stubs) — the highest-value
  harness work; test42 needs a NEW subject (its module is gone, so it is a scenario-design
  decision, not a rename). Candidate: the governed-moment/gate live path that replaced
  `conformance.ts`.
- Keep running the full suite on no-bump runs (GR-09).
- RE-58 flips only when a release contains the ceiling; verify with the four-arm probe.

---

## Run 119 — 2026-09-18 16:00 AEST — harness re-baseline (the run-118 follow-up, discharged)

**Gate.** `NO_NEW_RELEASE 1.11.0` confirmed directly at run start: installed
`/Users/codalee/.local/share/monet-node22/lib/node_modules/@team-monet/monet` = 1.11.0, npm
`latest` = 1.11.0, `main` HEAD `9fa38c2`, PR #155 `state=OPEN` / `mergedAt=null`. No bump →
no pivot → the run spent itself on the one open item run 118 handed over.

**Measured before → after (GR-09, `bash harness/run_suite.sh`):**

| | PASS | XFAIL | XPASS | STALE | FAIL |
|---|---|---|---|---|---|
| run 118 | 34 | 12 | 8 | **4** | 0 |
| run 119 | **37** | 12 | 8 | **1** | 0 |

### 1. test43_db_index → 30/30, exit 0

`cli/src/db/index.ts` no longer exports `getGateMirrorPath` (deleted),
`getGateJournalPath` → `getMomentSpoolPath`, and `getStartupFailurePath` is new. Two
*structural* fixes so the same drift class cannot return:

- the hand-copied constant stub became a **generated barrel over the REAL core modules**
  (`E2E_CORE_STUB` → `E2E_CORE_BARREL`; `MOMENT_SPOOL_FILENAME` is now read from
  `core/src/moment-spool.ts`, `STARTUP_FAILURE_SUFFIX`/`startupFailurePath` from
  `core/src/startup-diagnosis.ts`). The old stub was *how this test went STALE*: a core
  rename left the driver building clean against a fiction;
- the JS build script embedded in Python string escapes became a **static env-driven
  template** (`tests/assets/db_index_build.mjs`), deleting the escaping fragility that had
  broken lines 98–101 mid-edit.

New scenario 8 pins the startup-failure sidecar: the file name is the store path with the
startup-failure suffix appended, and it is **per-store distinct**.

### 2. test46_retrieval → 32/32, exit 0

`scoreSourceConcepts` was deleted by #61 `6c3d9c6` ("retire source subsystem"), so `SS1..SS6`
went with it. But #61 also removed the `kind != 'source'` native-read guards, which turned
`s6` into an **inversion rather than a breakage**: a `kind='source'` observation is now
SCORED by the native arm and wins attribution — measured 1.0 on observation `o1` by running
the re-baselined driver, not inferred from the diff. That is a behavioural change on the
retrieval hot path and is the load-bearing finding of this run.

### 3. test49_resolution_dedup → 47/47, exit 0

#52 / RE-47 retired the `kind=="correction"` ambiguous-band exemption. Driver header and
section 7 rewritten to **kind-parity** (`tauAmbiguous <= obsScore < tauAttach` →
`ambiguous-fork` for EVERY kind). The harness's `KNOWN_DRIFT` allow-list was **deleted** so a
future `corr_*` regression cannot hide behind it.

### 4. test42_conformance → stays exit 5, RE-SCOPED (not re-baselined)

`core/src/conformance.ts` AND `core/src/gate-journal.ts` are both absent, and
`computeConformance` / `runConformancePass` exist **nowhere** in the monorepo (0 hits). The
journal-verdict domain was not renamed — it was *replaced* by the moment-conformance model:
`momentConformance` over a governed-moment spool, surfaced by the
`conformance_ask` / `conformance_answer` tools. Successor pre-verified for the next run:
**`core/src/moment-ledger.ts`** (1577 lines, 13 exports — `createMomentTables`,
`migrateMomentColumnsFrom`, `foldMomentSpool`, `readGovernedMoment`, `observedMomentLosses`,
`momentLossCount`, `momentConformance`, `momentCounts`, `momentRuleReadsByStage`,
`momentsOwingAQuestion`, `attachMomentAsk`, `attachMomentAnswer`, + `MOMENT_SCHEMA_SQL`), which
is **DB-backed via `StoragePort`** ⇒ it needs a store fixture, i.e. a NEW scenario, not a
re-pointed pure driver. The test's docstring + STALE branch now carry that route so the next
run does not re-discover it; it stays loud (never green, never red).

**Issue:** team-monet/monet-e2e#35. **No product source touched** (GR-01/GR-02). Harness tree
committed + pushed. Scenario coverage unchanged (100%); this was a harness repair, not a
coverage run.
