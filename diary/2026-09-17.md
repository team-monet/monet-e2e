# 2026-09-17 — run 116 (worker, standby + RE-58 flip contract pre-registered against #155's new HEAD)

Run source: cron, scheduled 08:00 AEST, actual start 08:00:13 AEST (dispatch on time).

## What was done

- Release gate `NO_NEW_RELEASE 1.11.0`, verified directly rather than from the script alone:
  installed `/Users/codalee/.local/share/monet-node22/lib/node_modules/@team-monet/monet/package.json`
  = **1.11.0**, npm `latest` = **1.11.0** (`dist-tags {latest: 1.11.0}`), `.release_handled` =
  **1.11.0** → aligned → **no bump → no re-suite** (DIRECTION v3 run 3 priority 2 + halt list
  respected). GitHub side unchanged: newest tag `v1.11.0`, latest release `v1.7.1`, `main` HEAD
  `9fa38c2` (2026-09-14, #153).
- Read `DIRECTION.md` (v3 governor run 3, 2026-09-17 07:37) and `supervisor-state.md`. The
  governor's standing instruction on the baseline path (use the literal
  `…/lib/node_modules/@team-monet/monet` path; there is no `node22-prefix` directory) is applied
  in this run's records.
- G-5 direct re-read of upstream state (never carried forward) turned up the run's one signal:
  **PR #155's HEAD moved** from `e81db7b` to **`770cd98`** (`2026-09-16T21:52:31Z` =
  2026-09-17 07:52 AEST — i.e. **20 minutes after the governor's 07:30 DIRECTION read**, which
  still records `e81db7b`). Still `state=OPEN`, `mergedAt=null`, base `main`,
  `mergeStateStatus=CLEAN`, `gh pr checks 155` → **4/4 green**. New commit:
  *"fix(core): keep the schema-version preflight side-effect-free on WAL stores"* —
  `core/src/engine.ts` (+4/−4), `core/src/storage.ts` (+50/−8),
  `core/src/__tests__/schema-version-ceiling.test.ts` (+83/−4).

## Why that change mattered to the E2E side (and what was done about it)

`storage.ts` is where `readStoredSchemaVersion` lives — the function whose **absence** is
RE-58's shipped-side signature — and its read strategy is what the guard's flip will land on.
So the pre-registered flip was re-checked against the new HEAD's actual shape instead of being
left as an assumption:

- **Flip signal unchanged (the REFUSAL only).** The engine-side refusal is untouched by
  `770cd98`: all 4 of its changed lines are a comment. The comment now cites **#156 by name**
  ("The CLI's pre-engine circle resolution still opens the store raw before this constructor
  can check it"), which validates the guard's deliberate refusal *not* to assert a write-free
  store.
- **New branch table (read from the PR diff; unreleased — inference, not measurement):**
  no sidecars → SQLite header, offset 60, no connection; `-wal` + `-shm` (no `-journal`) →
  readonly connection peek (as before); **any other shape → `null` → the live port decides**.
- **Fixture shapes measured this run** (`/tmp/re58_sidecar_probe_r116.py`, isolated temp
  stores): arm A (bare stamped store, stdlib clean close) → `monet.db` only,
  `journal_mode=delete`; arm B (real store built by an MCP session, harness `close()`) →
  `monet.db` + `moments.jsonl`, `journal_mode=wal` with the WAL checkpointed and **both
  sidecars removed**. Both arms therefore present **no sidecars** → after the fix they take the
  new HEADER branch, i.e. a *conclusive* preflight — the guard does not silently depend on the
  live re-check. Stray shapes (`-wal` without `-shm`; `-journal` present) route to `null`.
- **Recorded:** a new §"Flip-time checklist" in `reverse-engineering/schema-migration.md`
  (branch table, measured shapes, and the three steps to run at flip time — require XPASS;
  additionally confirm the refusal on the stray-sidecar shapes test58 does not cover; report
  residue as a measurement, never assert write-freeness). `ISSUES.md` RE-58 row updated with
  the 2026-09-17 re-read and the checklist pointer.
- Shipped-artifact drift check (GR-08 baseline), re-measured this run on the served 1.11.0
  bundle (sha256 `0c579e235545119135b1…`): `readStoredSchemaVersion` **0**,
  `refusing to open` **0**, `newer than this build` **0**,
  `readSchemaVersionFromSqliteHeader` **0**, `newer than supported schema` **2** (repair
  refusal only) → the new commit is definitively **not** in the served build; RE-58's
  reproduction on dist 1.11.0 stands unchanged.

## Not done (by direction)

- No new test, no coverage re-measurement (coverage COMPLETE: 100% scenario / 83.8% line), no
  full-suite re-run (bump-gated).
- **No re-correction of the G-5 record**: the governor closed that as complete on 2026-09-17 and
  explicitly forbade re-doing it; this run only *added* new dated upstream facts (HEAD hash,
  commit, CI) and the flip-time checklist.
- No product source touched; no upstream triage outside the one PR this RE's flip depends on.
- No GitHub issue: nothing new (RE-58 tracked by upstream #107; #156 already carries the
  circle-map residue; #155's disposition remains a John decision).

## Metrics

Unchanged: Tests **58**, XFAIL **12**, XPASS 8, FAIL 0. Scenario coverage 100%, line coverage
83.8%. Registry: 58 rows = confirmed 12 / fixed 8 / source 18 / removed 7 / by-design 13, open 0.

## Next steps

- **Next `@team-monet/monet` release bump** (npm `latest` > 1.11.0): full-suite re-verify, then
  flip check RE-17/test24, RE-55/test56, RE-57/test57, **RE-58/test58** — the last only if that
  release actually carries the ceiling change, and then following the RE-58 flip-time checklist
  in `schema-migration.md`.
- Watch signal at that bump: #155's disposition (OPEN today, HEAD `770cd98`, 4/4 green) — if
  still open, the ceiling is absent from `main` and RE-58 stays XFAIL.
- **Cross-lane note (governor/dev lane, not E2E's to act on):** because #155's HEAD advanced
  after the 2026-09-17 07:30 DIRECTION read, the merge gate's "reviewed HEAD `e81db7b`" is no
  longer the branch's HEAD — the gate subject and the review-budget accounting changed.
- test09 burst flake remains the sole open environment item (observation only).

---

# 2026-09-17 — run 117 (worker, `standby` + RE-58 flip contract refined with the round-4 review's measured evidence; G-11 closed)

Run source: cron, job `7b218518e24b`, scheduled 16:00 AEST, actual start 16:00:09 AEST
(`source=builtin`, on time).

## What was done

- Release gate `NO_NEW_RELEASE 1.11.0`, verified directly rather than from the script alone:
  installed `/Users/codalee/.local/share/monet-node22/lib/node_modules/@team-monet/monet/package.json`
  = **1.11.0**, npm `latest` = **1.11.0**, `.release_handled` = **1.11.0** → aligned → **no bump →
  no re-suite**. Served bundle sha256 `0c579e235545119135b1…` unchanged. GitHub side: newest tag
  `v1.11.0`, latest release `v1.7.1`, `main` HEAD `9fa38c2` (2026-09-14, #153) → ceiling absent from
  `main`; open PRs = #155 + dependabot #152; no new issue since 2026-09-15.
- **G-5 direct re-read:** PR **#155 unchanged** — `state=OPEN`, `mergedAt=null`, HEAD **`770cd98`**,
  base `main`, `mergeStateStatus=CLEAN`, `gh pr checks 155` **4/4 pass**. No new HEAD, no merge.
- **The run's one new fact (read from the dev lane's artifacts, not asserted from memory):** John's
  approved **one-round waiver was consumed on exactly this HEAD** — the round-4 substitute review was
  posted with **`verdict=findings` (2×P2 + 2×P3)** and the **merge gate is withheld**; the dev lane
  records `blocked` and no further HEAD is authorized without a new ruling. Consequence for this lane:
  the ceiling is in no release, the flip precondition is **no closer**, and **RE-58 stays XFAIL**.
- **Flip contract refined (the deliverable).** Run 116 pre-registered the flip procedure from the
  HEAD's diff; this run adds the *measured* upstream evidence as an explicit
  **§Flip-time assertion constraints** section in `reverse-engineering/schema-migration.md`
  (everything labeled upstream-reported/unreleased, nothing re-measured here):
  1. the `null`-preflight branch is the fix's **least-verified** path — on `770cd98` no committed test
     can make `readStoredSchemaVersion` return `null` (0 mocks / no `-journal` / no asymmetric fixture
     in `schema-version-ceiling.test.ts`; all 11 tests decide via the header, the `-wal`/`-shm` peek,
     or a caller-supplied port) ⇒ the checklist's **stray-shape probe is the only coverage that path
     has**, not optional polish;
  2. on `null` shapes the ceiling is decided **only after the write port opens** (`engine.ts:2971`
     before `2974`), so a locking peer can block it (reviewer-measured: `journal_mode = WAL` blocked
     **8147 ms** behind an 8 s exclusive holder) and the refusal can surface as
     `(locked): database is locked` ⇒ at flip time a lock result is **inconclusive** (neither
     flip-failure nor flip-success) and stray probes must be lock-free/single-writer;
  3. the fix is **shape-dependent, not universal** — `-journal`/asymmetric stores are WAL-converted and
     modified **before** the refusal (dev lane measured header bytes 18/19 `1,1`→`2,2`, `-journal`
     removed, `-wal`/`-shm` created on a `user_version=14` store) ⇒ assert the **refusal** only, never
     write-freeness, on any shape (run 116's constraint, now with a measured cause);
  4. that residue is recorded **only** in #155's PR-body known-gaps section — **no issue tracks it**
     (#107 = ceiling absent, #156 = CLI circle-map path). Flagged for the dev lane; **E2E does not
     file** (halt list: no upstream triage; same defect family).
- Shipped-side baseline (GR-08) re-measured on dist 1.11.0: `readStoredSchemaVersion` **0** /
  `refusing to open` **0** / `newer than this build` **0** / `readSchemaVersionFromSqliteHeader` **0**,
  `newer than supported schema` **2** (`monet repair` refusal only) → the fix is definitively **not**
  in the served build; RE-58's reproduction stands.
- **G-11 closed from the worker side.** The pilot contract names `artifacts/agent-diary.md`; it did not
  exist because the diary has always been `diary/YYYY-MM-DD.md` (supervisor flagged this as a permanent
  false negative and explicitly left it as worker work). Created `artifacts/agent-diary.md` as a short
  per-run digest that points at the canonical daily file — the contract-named path now resolves, and no
  convention changed on the canonical record. One-off fix, not a recurring step.
- `ISSUES.md` RE-58 row annotated with the run-117 re-read + the withheld-gate fact (so the row no
  longer implies a merge is imminent).

## Not done (by direction)

- No new test, no coverage re-measurement (COMPLETE: 100% scenario / 83.8% line), no full-suite re-run
  (bump-gated), no re-correction of G-5 (governor closed it), no upstream triage beyond the one PR this
  RE's flip depends on.
- No product source touched. No GitHub issue filed (duplicate risk; #107/#155/#156 cover the family).
- Prod `~/.monet` never opened; all work read-only on the served build + record files.

## Metrics

Unchanged: Tests **58**, XFAIL **12**, XPASS 8, FAIL 0. Scenario coverage 100%, line coverage 83.8%.
Registry: 58 rows = confirmed 12 / fixed 8 / source 18 / removed 7 / by-design 13, open 0.

## Next steps

- **Next `@team-monet/monet` release bump** (npm `latest` > 1.11.0): full-suite re-verify, then flip
  check RE-17/test24, RE-55/test56, RE-57/test57, **RE-58/test58** — the last only if that release
  actually carries the ceiling change, and then following the RE-58 flip-time checklist **plus** the
  run-117 assertion constraints in `schema-migration.md`.
- Watch signal at that bump: #155's disposition (**OPEN, HEAD `770cd98`, round-4 `findings`, gate
  withheld** as of 2026-09-17 16:00) — while unmerged, the ceiling is in no release and RE-58 stays XFAIL.
- **Cross-lane note (governor/dev lane, not E2E's to act on):** the measured shape-specific residue
  (`-journal`/asymmetric conversion before the refusal; untested `null` branch; ceiling decided after
  the write port opens) lives only in #155's PR body — closing, rebasing or replacing #155 loses it,
  and no issue tracks it.
- test09 burst flake remains the sole open environment item (observation only).

### Direct verification addendum (same run, ~16:25 AEST) — review claims re-measured, not trusted

Two of the round-4 review's claims were re-verified by DIRECT read of the PR
branch source at ref `770cd98` (read-only fetch; unreleased → deliberately NOT
treated as the GR-08 baseline, which stays dist 1.11.0):

- `packages/core/src/__tests__/schema-version-ceiling.test.ts` @ `770cd98`:
  343 lines, `  it(` → **11**, `mock|spyOn` → **0**, `-journal|asymmetric` → **0**.
  ⇒ the `null`-preflight branch really has **no committed test**; test58's
  stray-shape probe is that path's only coverage.
- `packages/core/src/engine.ts:2967-2968` / `2980-2981`: the refusal message is
  `Store schema ${v} is newer than supported schema ${MONET_SCHEMA_VERSION};
  refusing to open. Upgrade Monet first.` ⇒ it contains the literal
  `newer than supported` that test58 asserts (`refusal_names_both_versions`), so
  there is **no false-failure risk from a message mismatch at flip time**. (Worth
  checking because the branch's own tests mix two expectation styles —
  `toBe(refusal())` for the `captureOpenError` fixtures vs `/newer than supported/`
  for the header/peek ones.)
- corroborated: `readStoredSchemaVersion` is defined at `storage.ts:811`
  (matches the run-116 citation).
