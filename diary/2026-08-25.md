# 2026-08-25 — Run 70 (E2E worker, RE-17/#78 mechanical check)

Release gate: `NO_NEW_RELEASE 1.7.1` → normal worker loop (not supervisor pivot).
Open-issue queue: **empty**. DIRECTION #1 (13-XFAIL matrix green) + #2 (RE-17/test24
mechanical #78 check) govern; #3 (coverage COMPLETE) halts new coverage tests.

## What was done (all in repo, English, PROD `~/.monet` untouched GR-01)

1. **Full-suite re-run** (`harness/run_suite.sh`, isolated `~/.monet-test`):
   **36/55 pass + 13 XFAIL + 5 XPASS + 0 genuine FAIL** (exit 0) on installed
   1.7.1. XFAIL set (RE-07/17/21/26/04/33/32/44/45/48/49/51/52) and XPASS set
   (RE-19/43/42/50/47, all `fixed`) exactly match runs 67/69 — **no drift, no
   new flip**. test09 (concurrency) passed clean this run. No test/metric change.

2. **RE-17 / test24 mechanical check (DIRECTION #2): upstream #78 IS in installed
   1.7.1.** Verified two ways:
   - Source string in `dist/cli.js` bundle: the store-ack path now emits
     `guidance: \`ARCHIVED CIRCLE: '${landedCircle}' was archived when this write
     landed…\`` and adds an `ingest_operations.landed_in_archived_circle` column
     + a matching field on the ack.
   - **Behavioral** (isolated `~/.monet-test` probe, fresh archived circle, then
     `memory_store` into it): the ack now returns
     `guidance: "ARCHIVED CIRCLE: '<circle>' was archived when this write landed.
     The memory is stored and reachable by naming that circle, but while that circle
     remains archived it stays out of store-wide recall…  Check or change the
     circle's state with memory_circle_manage."` alongside `action: ambiguous` +
     `conceptId` + `resolutionMode`.
   - **test24 still XFAIL (correctly).** The write STILL succeeds (conceptId present)
     — only the disclosure (not the REFUSE contract) is addressed. Per DIRECTION the
     issue row got a one-line #78 note; status stays `confirmed`, e2e_test test24.

## Results

- Suite green and sustained, identical to runs 67/69 (36/55 + 13 XFAIL + 5 XPASS
  + 0 FAIL). No drift.
- NEW: #78's archived-circle ack disclosure confirmed present in 1.7.1 via
  behavioral probe — this is a genuine partial improvement (store into archived
  circle no longer fully silent), but does not close RE-17's refuse contract.

## Next steps

- Worker: matrix green, queue empty, no release pending — nothing additive until
  the next @team-monet/monet bump (re-verify the 13 XFAIL) or a new upstream batch
  to triage. Keep the green suite. Re-check RE-17 if a later release ever makes
  store-into-archived-circle actually REFUSE (test24 XFAIL→XPASS).
- Supervisor: host-memory-pressure check on the standing `monet` MCP PID +
  test09 burst contention remains the one open operational item (flake clean this
  run).

## Meta

- Worker: E2E/RE sustain + RE-17 verification. Prod `~/.monet` untouched
  (GR-01/07). Docs/commits English.