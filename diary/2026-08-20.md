# 2026-08-20 — Run 51 (E2E / coverage): core/conformance.ts pure-function driver (issue #18, DIRECTION #1)

## Priority taken

DIRECTION (supervisor run #8) priority #1 was `[E2E] core/conformance.ts line
coverage scenario` (monet-e2e#18): the largest non-deprecated core gap at 33.1%
(169/510). The prescribed route was a "`monet gate` evaluate path". Reading the
source first made clear that route does **not work**: `engine.runConformancePass`
— the sole caller of `computeConformance` — is **not wired to any command or
tool on either the shipped 1.6.3 binary or `main`** (verified: grep finds only
its definition in the source tree; the §5 "callable now, scheduled when the
consumer exists" comment confirms it). `monet gate` writes gate-journal
disposition lines but never runs the pass that consumes them.

So the conformance "cheap half" — the §4/§5 verdict decision table — is
reachable only as a pure function. This run covers it with a **direct driver**
bundled from core SOURCE (esbuild, alias `@conformance-src` →
packages/core/src/conformance.ts, no native deps), run under the same
coverage-attribution mechanism as the CLI/MCP suite.

## What was added

- **`tests/assets/conformance_driver.mjs`** (47 assertions) — drives
  `computeConformance`, `tallyByRule`, `retirementCandidates`, and
  `appendConformanceAnnotations` with hand-crafted journal lines and pins the
  exact outputs (all falsifiable, GR-07). The product code behaved as designed
  on every scenario after two fixture corrections (a chain-fold case where I
  first forgot to link the wrapper into the chain via `parentId`, and a
  retry-improvement case where the second occurrence is legitimately its own
  fire event).
- **`tests/test42_conformance.py`** (9 harness assertions) — builds the driver
  bundle (esbuild build script written into `packages/cli`, mirroring the
  `coverage-build.mjs` recipe so esbuild resolves), runs it with node@22,
  asserts `RESULT: 47 passed, 0 failed`. Bundle output goes to a stable
  gitignored `build/conformance.coverage.js` so cov-map can read it back after
  a `NODE_V8_COVERAGE` run. No store, no embedder, no `~/.monet` touched (GR-01).
- **`scripts/cov-map.cjs`** (skill) — generalized from single-bundle to
  MULTI-BUNDLE (`COV_BUNDLES` comma list): coverage from any bundle (the
  `cli.coverage.js` MCP bundle AND the pure-core `conformance.coverage.js`
  driver) is unioned per module short name. This is the durable primitive for
  future pure-core modules with no MCP/CLI surface.

## Conformance contract pinned (the §4/§5 verdict decision table)

deny + enforced → `changed`, claimType `source-observed`; single-rule deny →
NO `verdictRuleIds` (nothing to apportion); mixed-severity deny with recorded
`blockingRuleIds` → `verdictRuleIds` = the blocking set; multi-rule deny with no
recorded set → `verdictRuleIds: []` (no rule credited) + reason appends "which
rules blocked is unavailable"; deny delivered-but-unenforced (`enforced:false`)
→ verdict ABSENT / claimType `unavailable` (a deny that never enforced is not
`changed` — the stage-lookup-advisory path); advisory fire → `unavailable`;
non-fire dispositions (silent / stage-hit-no-rules) ignored; multi-mouth
`parentId` chain folds to ONE verdict + `enforced` read across the whole chain;
retry detection: same act (`actionContext` || `actionContextSha256`) in a later
DIFFERENT chain → `retriedUnchanged:true`; `buildChainIds` multi-hop
(grandchild→root) so one evaluation is not a false retry; idempotence (prior
conformance line suppresses re-annotation) EXCEPT on retry-improvement and
scope-migration; `tallyByRule` full verdict vocabulary
(changed/conformed/breached/no-effect/vacuous) + per-rule scoping +
`awaitingJudgment`; `retirementCandidates` = fires>0 && changed+conformed==0 &&
awaitingJudgment==0 (no-effect counts as measured; all-awaiting and never-fired
excluded); `appendConformanceAnnotations` writes `phase:'conformance'` lines.

## Coverage result

- **core/conformance.ts: 33.1% → 81.6%** (169 → 416/510) — the largest
  non-deprecated core gap cleared.
- **Total line coverage: 82.9% → 83.3%** (44,744 → 44,991 / 53,987).
- Full suite under the coverage bundle: 26/42 pass + 15 xfail + 1 xpass + **0
  FAIL** (test42 included, green on installed 1.6.3 AND the coverage bundle).
- Remaining pipeline: cli/db/index.ts 27.9%, core/spans.ts 39.8%,
  cli/circle.ts 37.8%, cli/bootstrap.ts 22.4%, cli/project-dir.ts 21.1%
  (deprecated source-* 3–52% + render-overview.ts 6.9% RE-37 excluded per
  DIRECTION).

## Issue handling

- **monet-e2e#18 (conformance coverage)** → COMPLETE. Commented + closed on
  GitHub.
- #59 (upstream, CLOSED) confirmation comment was already added in run 50 —
  no repeat (DIRECTION line-17 item confirmed done).

## Notes / pitfalls

- The multi-bundle cov-map keyed by module short name means the same source
  reached from two bundles (cli.coverage.js + a pure-core driver) unions into
  one coverage row — no double count (Set of line numbers).
- A pure-core driver imports ONLY the module(s) it exercises (conformance.ts +
  gate-journal.ts are pure, node-builtins only), so the bundle is self-contained
  — no need to copy into the installed package's dist for native-dep resolution.
- test42 writes its esbuild build script into `packages/cli` (so esbuild from
  that pnpm repo resolves, the same constraint as `coverage-build.mjs`) and
  removes it in a `finally`.

## Next steps

- Coverage pipeline: cli/db/index.ts (27.9%, a small path-resolution module —
  many CLI subcommands `import` it, likely a quick win) or core/spans.ts (39.8%).
- Re-verify the 15 XFAILs + upstream #28/#37/#49/#50 at the next
  `@team-monet/monet` release (npm still 1.6.3; `main` ahead).

## Meta

- Supervisor: run #8 (2026-08-20). Worker: E2E.
- Suite run under the coverage bundle (cli.coverage.js from `main` +
  conformance.coverage.js driver).
- Env healthy; prod `~/.monet` untouched (GR-01).
