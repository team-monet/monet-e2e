# 2026-08-18 — Run 43 (RE): upstream #19/#20 independent verification → RE-45 + RE-46

## Why this run

DIRECTION.md (supervisor run #5) named priority #1:

> **[RE] upstream #19/#20 독립 검증 → RE-NN 등록** (monet-e2e#11): storage.ts
> was documented in run 38, but the specific findings in upstream #19 (transient
> `database is locked` — 5s busy_timeout starved by multi-minute write bursts;
> `memory_fetch` is a hidden writer) and #20 (a query can hold the write lock
> forever — better-sqlite3 has no `interrupt`) were never independently
> registered as RE-NN. Verify both claims against readable `storage.ts` +
> `engine.ts`, register them, and judge whether #19 is E2E-reproducible as a
> deterministic concurrency XFAIL. storage.ts must NOT be re-documented
> (verification only).

The only open monet-e2e issue was #11 (this exact task). This is the last
unclaimed RE surface — all 24 modules are otherwise documented.

## What was done

1. Read DIRECTION.md, METRICS.md, GUARDRAILS.md, ISSUES.md, storage.md,
   diary/2026-08-17.md.
2. Read upstream `team-monet/monet` #19 and #20 in full.
3. Verified both claims against the current readable monorepo source
   (`~/monet/monet/packages/core/src/`): `storage.ts`, `engine.ts`,
   `embedding-state.ts`, and the installed `better-sqlite3` driver.
4. Ran a minimal deterministic contention probe (isolated temp DB) to confirm
   the contention mechanism empirically.
5. Registered RE-45 (#19) and RE-46 (#20) in ISSUES.md; flagged RE-45 as E2E
   follow-up test35; recorded the run in METRICS.md; commented + closed
   monet-e2e #11.

## Verification evidence (current source line numbers)

- **`busy_timeout = 5000`** — `storage.ts:253`. Nuance at `storage.ts:226-230`
  (the #215 note): better-sqlite3 arms `sqlite3_busy_timeout` from its own
  `timeout` option at open (default 5000), and the explicit pragma runs AFTER
  the wait it looks like it governs.
- **`memory_fetch` is a hidden writer** — `engine.ts:5054-5056`: `getConcept`
  runs `UPDATE concepts SET usefulness_score = usefulness_score + 1,
  usefulness_last_fetched_at = ? WHERE id = ?` with no SQLITE_BUSY catch and no
  transaction; `engine.ts:5057-5059`: `if (synthesizedNow) row = await
  this.synthesizeRow(row)` — a dirty concept is inline-synthesized on fetch.
  Either write failing under contention fails the entire fetch. (Upstream cited
  `engine.ts:3330-3334`, now drifted to 5054-5059.)
- **`checkpoint()` synthesizes every dirty concept in one loop** —
  `engine.ts:5106-5107`: `SELECT * FROM concepts WHERE dirty = 1 … .all(circle)`
  then `for (const r of rows) await this.synthesizeRow(r)` — no yield/batch/cap.
- **No `interrupt`/progress handler** — better-sqlite3 11.10.0 prototype
  methods: `constructor prepare transaction pragma backup serialize function
  aggregate table loadExtension exec close defaultSafeIntegers unsafeMode`.
  `busy_timeout` bounds *waiting*, not *holding*.
- **`inspectStoredEmbeddingRows` materializes full embeddings** —
  `embedding-state.ts:128`: `readLiveEmbeddingRows` returns
  `db.prepare(LIVE_EMBEDDING_SQL[population]).all()` over `SELECT id, embedding`
  (the full JSON column), folded in JS afterward at `:131`. Memory grows
  linearly with population size before the reduction.

## Deterministic contention probe (empirical, isolated)

A second better-sqlite3 connection writing while another holds
`BEGIN IMMEDIATE` fails `SQLITE_BUSY` ("database is locked") after **~11.8 s**,
not 5 s — in WAL mode the open-time `timeout` default and the explicit
`busy_timeout=5000` pragma stack. This confirms the contention mechanism is
deterministic and pins the safe hold-time (≥12 s) for a future test. Probe
script lived in `/tmp` (not the repo), DB in a temp path — no isolation leak.

## #19 E2E-verifiability judgment

**REPRODUCIBLE** as a deterministic scenario-9-extension XFAIL. Recipe:
isolated store with a live concept → a raw better-sqlite3 connection holds
`BEGIN IMMEDIATE` (one tiny write) for ≥12 s → MCP `memory_fetch` on the
concept → its usefulness-bump UPDATE blocks → the whole fetch fails
`database is locked`. Desired contract: *a fetch (read) must not fail on a
telemetry write — the bump should be best-effort and the concept still
returned.* **Flagged as test35** (NOT implemented — verification-only scope,
per DIRECTION). RE-45 stays `open`.

## #20 status

`source` (structural). "No interrupt" is a driver-API property (not
MCP/CLI-observable without a deliberately bad query); the `.all()`
materialization is a scalability observation, not a clean behavioral XFAIL.
Routes to the code-fix queue, not the E2E verifier.

## Metric changes

- Tests: 34 (unchanged). XFAIL: 12 (unchanged). Coverage: 100%. Failure: 0%.
- Assertions: 422 (unchanged).
- RE registry: 44 → 46 (RE-45 `open` S2, RE-46 `source` S2).

## Next steps

- **Next E2E run: implement test35** (RE-45 → XFAIL) using the flagged
  `BEGIN IMMEDIATE`-holder recipe (≥12 s hold). This converts RE-45 `open` →
  `confirmed` and joins the L2 code-fix queue.
- Re-verify all 12 XFAILs + RE-31 at the next `@team-monet/monet` version bump
  (npm still 1.6.3, prod 1.6.1 — no bump this run).
- RE-46 (structural) joins RE-42 in the source/structural code-fix backlog for
  John's prioritization.

## Meta

- Supervisor: run #5 (2026-08-18). Worker: RE (reverse-engineering).
- No suite run this time (no version bump, no behavioral source change; the
  verification is source-level + one isolated probe).
- storage.ts was NOT re-documented, per DIRECTION ("검증만").
- GitHub issue #11 (monet-e2e) processed: verified, registered, commented +
  closed.

---

## 2026-08-18 — Run 44 (E2E): RE-45 → XFAIL test36 (memory_fetch hidden writer)

### Why this run

DIRECTION.md (supervisor run #6) priority #1: **RE-45 → XFAIL test36**
(monet-e2e#12). run 43 verified the deterministic reproduction
(`BEGIN IMMEDIATE` ≥12s hold → `memory_fetch`'s usefulness-bump UPDATE blocked
→ fetch `database is locked`). test35 is already occupied by config-cli, so
RE-45 uses **test36**. Complete = `open`→`confirmed`, join L2.

### What was done

1. Read DIRECTION.md, METRICS.md, GUARDRAILS.md, ISSUES.md, L2 queue,
   `mcp_client.py`, test33/test34/test35 templates, run-43 diary.
2. Wrote `tests/test36_re45_fetch_hidden_writer.py` (RE-45 → XFAIL).
3. Ran the full suite: **22/36 pass + 13 xfail + 1 xpass + 0 fail** (exit 0).
4. Updated ISSUES.md (RE-45 `open`→`confirmed`, `e2e_test` `—`→test36),
   L2-code-fix-queue.md (RE-45 → active S2; also fixed the stale RE-43/RE-44
   rows still listed `open` in the pending-confirmation table despite being
   confirmed in run 40), and METRICS.md (run 44 row).

### test36 mechanism (deterministic, ~20s)

- Spawn a Monet server on a fresh isolated temp store; store ONE concept;
  control `memory_fetch` returns the card
  (`{body,circle,id,kind,lastConfirmedAt,needsSynthesis,observationCount}`).
- Open a SECOND connection to the same `monet.db` with Python stdlib `sqlite3`
  (`isolation_level=None`), issue `BEGIN IMMEDIATE` + `CREATE TABLE` + `INSERT`
  (a real write so the WAL write lock is held), hold it 20s.
- While held, `memory_fetch` (in a thread) blocks on its unprotected
  usefulness-bump UPDATE for the stacked busy_timeout and returns
  `fetch failed: database is locked` (observed elapsed ≈12.6s).
- DESIRED contract (asserted): the fetch must return the concept — the bump
  should be best-effort. Bug present → exit 2 (XFAIL); fixed → exit 3 (XPASS).

### Key finding

RE-45 is a **hidden-writer** bug: `memory_fetch` → `getConcept` runs an
unprotected `UPDATE concepts SET usefulness_score = usefulness_score + 1, ...`
BEFORE assembling the read result (`engine.ts` ~5054-5056), no SQLITE_BUSY
catch. A competing writer's burst makes a pure read report `database is locked`
— the telemetry write takes down the read it was only supposed to annotate. S2.

### Metric changes

- Tests: 35 → 36 (test36). XFAIL: 12 → 13. XPASS: 1 (test23/RE-19, unchanged).
- Coverage: 100% (scenario basis). Failure: 0%.
- Assertions: 422 → 441 (test35 +14 was unrecorded in the core table; test36 +5).
- RE registry: RE-45 `open` → `confirmed` (46 issues total, unchanged).

### Next steps

- Re-verify all 13 XFAILs at the next `@team-monet/monet` version bump (npm 1.6.3).
- DIRECTION priority #2 (RE): upstream #52 auto-merge triage (monet-e2e#13).
- DIRECTION priority #3 (E2E): install-cli.ts coverage scenario (monet-e2e#14).
- RE-45 joins the L2 queue (now 10 confirmed: RE-26/43/44/45 S2, RE-17/07/04/33 S3, RE-21/32 S4).

### Meta

- Supervisor: run #6 (2026-08-18). Worker: E2E.
- GitHub issue #12 (monet-e2e) processed: test36 implemented + committed,
  commented + closed.

---

## 2026-08-18 — Run 45 (E2E): install-cli.ts coverage scenario (test37)

### Why this run

DIRECTION.md (supervisor run #6) priority #3: **install-cli.ts coverage gap**
(monet-e2e#14) — 1345 uncovered lines at 5.3%, the largest non-deprecated CLI
target. Priorities #1 (RE-45 -> test36, run 44) and #2 (RE upstream #52 triage,
monet-e2e#13 — owned by the RE worker) are handled/assigned. `monet install` is
the client's Claude Code PreToolUse hook installer, a real side-effectful
surface the MCP stdio path never touches, which is why it sat at 5.3%.

### What was done

1. Read DIRECTION.md, METRICS.md, GUARDRAILS.md, ISSUES.md, L2 queue,
   coverage-gaps.md, `install-cli.ts` (full 1420 lines), `circle.ts`,
   `db/index.ts`, `cli.ts`, test35 (the CLI-subcommand template) and
   `mcp_client.py` (`run_cli`).
2. Wrote `tests/test37_install_cli.py` — 9 arms, 41 assertions.
3. Ran it standalone: 41/41 PASS. Then the full suite: 23/37 pass + 13 xfail +
   1 xpass + 0 fail (exit 0), identical to run 44 (no regression).
4. Re-measured source line coverage (coverage-build.mjs -> cli.coverage.js ->
   full suite under NODE_V8_COVERAGE -> cov-map.cjs) to confirm the gap closed.
5. Updated METRICS.md, coverage-gaps.md (regenerated by cov-map.cjs), and this
   diary; commented + closed monet-e2e#14.

### Isolation (the key risk)

`monet install` writes `~/.monet/gate-hook.mjs` (via `homedir()`), a settings
file (project-local or `~/.claude/settings.json` with `--user`), and — for
project scope — calls `ensureMonetDir` + `deriveCircle` (which opens SQLite
read-write and creates `remote_circle_map`). Isolation is the HOME-redirect
technique (test25 precedent) PLUS `MONET_STORAGE_DIR` (which `getMonetDir`
checks FIRST, so the store write is redirected too) PLUS `MONET_CIRCLE` (pins
the circle so `deriveCircle` returns immediately, no git-remote lookup against
the parent `~/.monet-test` repo). Every arm uses a fresh `TemporaryDirectory`;
no embedder/model is loaded by `monet install` (pure file-writing command), so
no MONET_MODEL_CACHE concern. Verified: wrapper/store/settings all land in the
temp tree; arm I (MONET_CIRCLE dropped) confirms the real deriveCircle path
writes `<store>/monet.db` + `remote_circle_map` to the isolated store.

### Arms

- **A** project scope (real write): wrapper 0755 + `settings.local.json` with
  anchored `^Bash$` / `^(Task|Agent)$` PreToolUse matchers + exec-form handler
  (`args = [wrapperPath, --circle, <pin>]`) + WIRED/NOT WIRED coverage report.
- **B** idempotent re-install + foreign-hook preservation (`isMonetGateHandler`
  full-path recognition; `upsertMonetGateHook` clean-then-add): second install
  leaves exactly 1 monet handler, preserves a user's `^Write$` group + a
  top-level key byte-for-byte.
- **C** `--user`: writes `~/.claude/settings.json`, NO `--circle` pin (per-
  invocation resolution).
- **D** `--dry-run`: prints what WOULD be written, writes nothing (readOnly
  deriveCircle).
- **E/F/G/H** refusals (rc 1, no write): nonexistent `--project`; invalid-JSON
  settings (file byte-unchanged); valid-JSON-wrong-shape (`hooks` as a string);
  `MONET_CIRCLE='*'` reserved wildcard.
- **I** real deriveCircle (no MONET_CIRCLE, non-git project): folder-hash pin
  (`project-<sha8>`) + store `monet.db` created with `remote_circle_map` table.

### Coverage result (the completion criterion)

`install-cli.ts`: **5.3% (75/1420) -> 75.5% (1072/1420)**. Total line coverage
**80.1% -> 81.9%** (43,840/53,505). The residual ~24.5% is `buildWrapperScript`'s
generated-wrapper template literal (static string data, not runtime code) —
mirrors config-cli.ts's own doc-comment residual. install-cli.ts's executable
code is now effectively fully covered.

### Metric changes

- Tests: 36 -> 37 (test37). XFAIL: 13 (unchanged). XPASS: 1 (unchanged).
- Coverage (scenario basis): 100% (unchanged). Failure: 0%.
- Assertions: 441 -> 482 (+41).
- Code line coverage: 80.1% -> 81.9%.

### Next steps

- DIRECTION priority #2 remains open for the RE worker: upstream #52 auto-merge
  triage (monet-e2e#13).
- Next E2E coverage target: `cli/gate-cli.ts` (1078 lines, 6.9%) — the last
  big non-deprecated CLI gap. `monet gate` needs a mirror to evaluate against
  (materialize a skeleton first), so it is heavier than `install`/`config`.
- Re-verify all 13 XFAILs at the next `@team-monet/monet` version bump (npm
  1.6.3, prod 1.6.1).

### Meta

- Supervisor: run #6 (2026-08-18). Worker: E2E.
- GitHub issue #14 (monet-e2e) processed: test37 implemented + coverage
  re-measured + committed, commented + closed.
