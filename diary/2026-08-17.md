# 2026-08-17 — Run 40 (E2E): RE-43 + RE-44 → XFAIL (repair self-deadlock + materialize dirty skeleton)

## Why this run

DIRECTION.md (supervisor run #4) named two priorities:

1. **[E2E] RE-43 → XFAIL** (GitHub issue #9): `monet repair` self-deadlocks on
   every English-only target (`recheckNonEnglish` opens a second connection under
   `applyRepair`'s exclusive lock → `SQLITE_BUSY`).
2. **[E2E] RE-44 → XFAIL** (GitHub issue #10): `monet materialize` renders a
   dirty/unsynthesized skeleton body as governing text.

Both were `open` + `e2e_test: —` in `reverse-engineering/ISSUES.md`. This run
converts them into behavioral XFAIL tests (the E2E verification loop), records
them as `confirmed`, and closes the two GitHub issues.

## What was done

1. Read DIRECTION.md (priorities), METRICS.md, GUARDRAILS.md, ISSUES.md, and the
   two open GitHub issues (#9, #10).
2. Read the readable-TS source to pin the exact trigger conditions before
   writing tests:
   - `repair-cli.ts` / `engine.ts`: `skeletonMemberRows` selects `c.body` with
     `status='active'` + latest `verdict IN ('approve','re-ratify')` and NO
     `dirty`/`needsSynthesis` guard (RE-44); `recheckNonEnglish` is constructed
     for `guardedMode && targetIsEnglishOnly && !acceptNonLatinLoss` (RE-43).
   - `db/index.ts`: `materialize` resolves its store via `MONET_STORAGE_DIR`
     (no `-d` flag) — the test sets that env var for isolation.
3. Reproduced both bugs in a throwaway temp dir before writing the tests
   (ground truth first, per the guardrails' data-driven rule):
   - RE-43: an all-English store + `repair --target Xenova/bge-small-en-v1.5
     --apply --yes` → rc=1, `database is locked`, backup retained (fails closed).
   - RE-44: declare a principle, re-declare near-identical content (`action=
     attached`) → `needsSynthesis=True`, `obs=2`, body = old+new concatenation;
     `materialize` emits BOTH paragraphs verbatim into the standing file, and
     the block `state` hash reflects that dirty body (so `mirrorStale` would
     report green).
4. Wrote `tests/test33_re43_repair_deadlock.py` and
   `tests/test34_re44_materialize_dirty_skeleton.py` (both isolated temp-dir,
   re-run safe, falsifiable).
5. Ran the full suite: **21 pass / 12 xfail / 1 xpass / 0 fail** — both new
   tests land as XFAIL (exit 2), no regression.

## Findings (verified behavior)

- **RE-43 (repair self-deadlock) — CONFIRMED.** `monet repair --target
  Xenova/bge-small-en-v1.5 --apply --yes` on an all-English store fails with
  `monet repair: Cannot inspect Monet store '…' (locked): database is locked`
  (rc=1) after the 5s busy_timeout. The backup is retained and the store stays
  pinned to `Xenova/bge-m3:cls:q8` — it fails closed, no rewrite. The store is
  all-English (0 non-Latin), so the one-way non-Latin guard correctly passes and
  `applyRepair` runs — exactly the path that deadlocks. DESIRED contract (what
  the test asserts): the repair should complete (rc=0) and repin.
- **RE-44 (materialize renders dirty skeleton) — CONFIRMED.** A principle
  declared then amended via a near-identical re-declaration attaches
  (`action=attached`, same conceptId), becomes dirty (`needsSynthesis=True`,
  2 observations), and its `body` becomes the unreconciled concatenation of the
  old and new paragraphs. `monet materialize` renders that concatenation
  VERBATIM as governing text (`# Principles` contains both paragraphs), and the
  manifest `blockHash`/`skeletonState` both hash the dirty body, so `mirrorStale`
  reports green. DESIRED contract (what the test asserts): the unreconciled
  concatenation must not reach the standing file — either refuse or render only
  a reconciled body.

## Metric changes

- Tests: 32 → 34.
- XFAIL: 10 → 12 (test33, test34 added).
- Coverage: 100% (unchanged — repair regression is scenario 8; materialize is a
  new surface beyond the original matrix, no scenario count change).
- Failure rate: 0% (both new tests are tracked XFAILs, not failures).
- Assertions: 413 → 422 (test33 +2, test34 +7).

## Next steps

- Re-check RE-43/RE-44 (and the other 10 XFAILs) at the next
  `@team-monet/monet` version bump; a flip to XPASS → update ISSUES.md status.
- RE-43/RE-44 are now `confirmed` (S2, behavioral) — they join the L2 code-fix
  queue (RE-26, RE-42) awaiting John's call; supervisor will promote them.
- No version bump available (npm still 1.6.3, prod at 1.6.1) — nothing to
  re-verify this run.

## Meta

- Supervisor: run #4 (2026-08-17). Worker: E2E.
- GitHub issues #9 (RE-43) and #10 (RE-44) processed: tests written, results
  recorded, issues commented + closed.
- Isolation: all store/test activity in `tempfile.mkdtemp` dirs (never
  `~/.monet`). `MONET_MODEL_CACHE=~/.monet/models` reused cached
  bge-m3 / bge-small-en-v1.5 (no re-download).

---

# 2026-08-17 — Run 41 (E2E): stability check + upstream duplicate cross-reference

## Why this run

DIRECTION.md priorities #1 (RE-43) and #2 (RE-44) were already completed in
run 40. Priority #3 (version-bump re-verify) is waiting — npm `@team-monet/monet`
is still 1.6.3 (prod at 1.6.1), no new version since run 32's check. No open
monet-e2e issues. No open behavioral RE issue left to convert (only RE-05, which
is blocked by the source deprecation path). This run is therefore a stability
confirmation plus an upstream sweep.

## What was done

1. Read DIRECTION.md, METRICS.md, GUARDRAILS.md, ISSUES.md, diary/2026-08-17.md.
2. Checked monet-e2e issues: none open (all 10 closed).
3. Checked npm: latest = 1.6.3, prod install = 1.6.1 — no bump, nothing to
   re-verify.
4. Ran the full suite as a stability check.
5. Swept upstream team-monet/monet open issues.

## Findings

- **Suite stable.** 21/34 pass, 12 xfail, 1 xpass, 0 fail — identical to run 40,
  no drift, no regression. The lone XPASS (test23, RE-19) is already recorded
  `fixed` — not a new signal.
- **Upstream duplicate batch (NEW cross-reference).** Issues #38-#48 (11 issues,
  all titled "slow-queries.jsonl is write-only — no doctor/CLI/MCP surface reads
  the slow log", filed 2026-08-16T21:35-21:37Z with differing area labels) are
  all the SAME underlying issue as RE-33 (already `confirmed`, test31 XFAIL).
  No new test is needed — test31 already reproduces it. Flagged for John: the
  upstream tracker carries 11 near-duplicate copies that could be consolidated
  into one.
- Remaining open upstream issues of note: #14 (RE-43, test33), #23 (RE-44,
  test34), #15 (RE-42, source/structural) — all already registered; no new
  behavioral surface discovered.

## Metric changes

- Tests: 34 (unchanged). XFAIL: 12 (unchanged). Coverage: 100%. Failure: 0%.
- Assertions: 422 (unchanged).

## Next steps

- Wait for the next `@team-monet/monet` version bump, then re-verify the 12
  XFAILs + RE-31 (any XPASS flip -> update ISSUES.md status to fixed).
- John may want to dedupe upstream #38-#48 (11 copies of RE-33).

## Meta

- Supervisor: run #4 (2026-08-17). Worker: E2E.
- Isolation: full suite runs in isolated temp dirs; `MONET_MODEL_CACHE` reused
  (no model re-download).
