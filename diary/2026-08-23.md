# 2026-08-23 — E2E worker (run 65, on installed 1.7.1)

## What and why

DIRECTION #1 for this worker was to triage the 2026-08-21 upstream monet issue
batch (#66/#68/#70/#72/#75/#81/#82/#83) — the "queue exhausted" transition-point
stall the supervisor flagged. No new release (gate: NO_NEW_RELEASE 1.7.1), so the
normal worker loop ran. I triaged all 8 issues against the INSTALLED 1.7.1 binary,
converting the two behaviorally-verifiable ones into XFAIL tests.

## Triage of the 08-21 upstream batch

**Behavioral → new RE + XFAIL (committed):**
- **#81 (area:store, sev:major) → RE-51 (S2), test54 XFAIL.** `memory_checkpoint`
  writes into an archived circle with NO disclosure. Reproduced on 1.7.1: store an
  anchor → `memory_circle_manage{archive}` → `memory_checkpoint{workstream}` on the
  archived circle → receipt `{circle, workstream:{id,title,opened,closed}}` with NO
  `guidance`/`archived` signal, and the DB `concepts` row (kind='workstream')
  genuinely lands in the archived circle name (outside store-wide recall). This is
  the checkpoint/save sibling of RE-17 (store path). NOTE: `monet start` DOES expose
  `memory_checkpoint` on the MCP surface (my probe's tools/list parse was just
  mis-shaped; the tool itself works).
- **#70 (area:cli, sev:minor) → RE-52 (S3), test55 XFAIL.** `monet install` crashes
  with an unhandled JS TypeError instead of refusing cleanly on a malformed
  `hooks.PostToolUse`. Reproduced on 1.7.1 with both shapes from the issue: seed a
  settings file with only `hooks.PostToolUse` malformed (PreToolUse absent →
  `validateSettingsShape` returns early `{ok:true}`) →
  `[{"matcher":"x","hooks":"not-an-array"}]` → rc=1 `i.filter is not a function`;
  `{"matcher":".+","hooks":[]}` → rc=1 `(t ?? []) is not iterable`. Desired
  contract: refuse cleanly like the PreToolUse wrong-shape/malformed arms
  (test37 F/G).

**Structural / no clean behavioral surface → routed L2:**
- **#68 (gate, major)** — malformed spool line consumed with no durable loss record;
  `moment_losses`/`gate.losses` are gate-ledger internals with no MCP/CLI-observable
  E2E assertion → L2.
- **#72 (gate, minor)** — future-version spool line parks the fold cursor, ledger
  frozen silently; requires hand-planted future-version spool lines + gate CLI →
  L2.
- **#75 (gate, minor)** — `momentStageReads` counts one lookup per rule; gate-internal
  metric inflation → L2.
- **#83 (gate, minor)** — moment spool ignores `writeSync` byte count (short write);
  trigger is a filling disk, not reliably creatable → L2.
- **#82 (store, minor)** — `storeContentionError` wraps only the connection open, so
  `init()`'s schema writes throw bare "database is locked"; requires holding the
  lock during MonetCore init (before any MCP tool call) — not a clean MCP XFAIL;
  RE-45/test36 already covers the contention surface → L2.
- **#66 (store, minor)** — a circle whose only content is governed moments cannot be
  renamed; needs gate instrumentation to create a moments-only circle (not reachable
  via MCP tools) → L2.

## Results

- Full suite on installed 1.7.1: **37/55 pass + 13 XFAIL + 5 XPASS (test23/33/51/52/53)
  + 0 genuine FAIL** (exit 0).
- The FIRST suite run's test09 burstB sub-phase failed (19/20 stores) under full-suite
  load; the second run's test09 passed clean standalone within the suite (15/15, and
  the suite itself exited 0). Confirmed the known environmental model-load memory
  contention flake, NOT a code regression (per skill + DIRECTION: re-run standalone
  before treating as real).
- XFAIL matrix grew 11 → 13: added test54 (RE-51), test55 (RE-52). XPASS steady at 5.
- Tests 53 → 55. Scenario coverage 100%; line coverage unchanged 83.8% (both new
  tests are behavioral bug-trackers, not coverage-lifters; no coverage run per
  DIRECTION COMPLETE).

## Notes / pitfalls

- `memory_checkpoint` is on the MCP surface but my first probe misparsed `tools/list`
  (used a guess at the result shape) and wrongly reported it absent. The checkpoint
  call itself worked — trust a real `memory_checkpoint` call over a tools/list parse.
- test55 exit-code framing: both crash shapes return rc=1 (top-level catch), so the
  XFAIL must key off the MESSAGE (`is not a function` / `is not iterable` TYPEERROR
  fingerprint), not rc. I split into `_rc1` (always passes) + `_clean_refusal` (fails
  under the bug) and translate "all clean_refusal fails + a crash reproduced" into
  exit 2.

## Next steps

- Worker: with the batch triaged, the two new open items (RE-51 S2, RE-52 S3) join the
  XFAIL matrix. Next real advance is the next @team-monet/monet release (re-verify),
  or a downstream triage if a new upstream batch lands. Keep the green suite + 13-XFAIL
  matrix on installed 1.7.1.
- Supervisor: promote RE-51/RE-52 into the L2 code-fix queue (S2 for checkpoint
  archived-circle disclosure is the highest-value; #68 also merits an L2 slot though
  it has no E2E surface). Re-check host memory pressure (permanent `monet` MCP PID +
  test09 burst contention flaked once again this run).
- GitHub: monet-e2e#26 (triage tracking issue) — this run completes the behavioral
  half (#81/#70 done); I'll leave it open for the L2 routing record or close after
  John's L2 call.

## Meta

- Worker: E2E. Prod `~/.monet` untouched (GR-01). Docs/commits English (GR-07).

---

# Appended 2026-08-23 (run 66 — RE) — checkpoint-circle + cli module docs, L2 promotion (08-21 batch closeout)

Release gate: `NO_NEW_RELEASE 1.7.1` → normal worker loop. Run 65 (E2E)
triaged the 08-21 upstream issue batch and confirmed two REs (RE-51, RE-52).
This RE run applied DIRECTION item #3 — the L2-promotion signal for the
now-confirmed batch — and closed the RE-side bookkeeping gap (the ISSUES rows
run 65 wrote pointed at module docs that didn't exist yet).

## What was done (all in repo, English, PROD `~/.monet` untouched GR-01)

1. **Module docs the ISSUES rows referenced (created).**
   - `reverse-engineering/checkpoint-circle.md` — the wired `memory_checkpoint`
     surface (mcp-server.ts:1816-1853: `scope(circle)` resolution,
     `saveWorkstream`/`captureFind`/`previewWorkstreamCheckpoint`,
     `trackedCheckpointHandler`, touch history) + the RE-51 detail. Grounded:
     the handler resolves the circle via `scope(circle)` and writes without ever
     consulting `isArchivedCircle` — the storeInternal guard from PR #78 is the
     only archived check, and checkpoint never goes through it.
   - `reverse-engineering/cli.md` — `monet install` / settings-shape surface
     (install-cli.ts:1019-1043 `validateSettingsShape`) + RE-52 detail.
     Grounded: `validateSettingsShape` validates ONLY `hooks.PreToolUse`
     (validates each group's `.hooks` array) then early-returns `{ok:true}` when
     PreToolUse is absent — so PostToolUse*/PostToolUseFailure values skip
     shape-validation and reach `upsertHandlerForEvent`, whose
     `group.hooks.filter(...)` throws (confirmed TypeError at test55).

2. **L2 code-fix promotion (DIRECTION item #3).** Added to
   `L2-code-fix-queue.md`:
   - **RE-51 (S2, test54)** in the Active S2 section — checkpoint writes into
     archived circle with NO disclosure (sibling of RE-17, upstream #81 sev:major).
   - **RE-52 (S3, test55)** in the Active S3 section — install crashes on
     malformed PostToolUse instead of refusing cleanly (upstream #70 sev:minor).
   - Active queue header 12 → **14 confirmed**.

3. **Registry/consistency.** ISSUES.md run-66 maintenance note; METRICS run-66
   row (modules 24→26, issues 48→52) + two module-inventory rows (checkpoint-circle,
   cli). Confirmed RE-51/RE-52 ISSUES rows were already well-formed from run 65.

## Results

- New module docs: 2 (`checkpoint-circle.md`, `cli.md`). Modules 24→26.
- No new RE-issue this run (RE-51/RE-52 confirmed in run 65, not in run 66);
  issue count 48→52 reflects run 65's confirmation.
- No threshold/param change (284 unchanged). No product behavior verified this
  run — pure RE bookkeeping/closure.

## Consistency check vs DIRECTION

- DIRECTION #3 ("#81/#68 확인 시 L2 승격 신호") — satisfied: RE-51/RE-52 promoted
  to L2; #68 already routed L2 (structural, no E2E surface) in run 65.
- DIRECTION halt rules respected: coverage pipeline COMPLETE (no coverage work),
  no new readable-TS module documentation pass (these two docs are
  issue-anchored closeout, required by the ISSUES rows that referenced them, not
  a fresh documentation sweep), source/removed maintenance untouched.

## Next steps

- Worker: the 08-21 batch is now fully closed on the RE side (triage run 65 +
  L2/docs run 66). Next real advance remains the next @team-monet/monet release
  (re-verify the now-14-XFAIL matrix). Keep green suite — 37/55 pass, 13 XFAIL,
  5 XPASS, 0 FAIL on installed 1.7.1.
- Supervisor: close/park monet-e2e#26 (triage tracking) after the L2 record;
  the host-memory-pressure check (permanent `monet` MCP PID + test09 burst
  contention) from run 64/65 is still open — worth a look before the next
  full-suite run.

## Meta

- Worker: RE. Prod `~/.monet` untouched (GR-01/07); no Monet MCP calls (cron
  rule 7). Docs/commits English.