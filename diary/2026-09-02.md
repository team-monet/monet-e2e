# 2026-09-02 — run 85 (worker, 1.11.0 release re-verify)

## Focus
DIRECTION #1: **`@team-monet/monet` 1.11.0 re-verify** (npm latest = 1.11.0, tag
v1.11.0). The release gate read `NO_NEW_RELEASE 1.11.0` because PROD is already
on 1.11.0: DIRECTION (written by run-84 supervisor on the 1.9.0 PROD) was stale
on the binary version — `~/.local/share/monet-node22` now hosts 1.11.0
(package.json `version: 1.11.0`) and `.release_handled` was rolled forward to
`1.11.0` by the gate script's installed==latest branch. But the re-verify had
NOT actually run on 1.11.0 (no `prefix-1110`, no 09-02 diary, git log ended at
run-84 on 1.10.0). So this worker run IS the 1.11.0 re-verify, run directly
against the installed 1.11.0 binary (isolated `-d` stores, GR-01 intact).

## What was done
- **Full suite vs installed 1.11.0 → 38/56 pass + 10 XFAIL + 8 XPASS + 0 FAIL
  (exit 0), tally byte-identical to run-84/1.10.0.** No tracked RE flipped either
  direction, as DIRECTION predicted (the conformance rework did not touch
  archived-disclosure territory). XFAIL steady on RE-07/17/26/32/33/44/45/48/49/55;
  XPASS steady on RE-19/21/04/42/43/47/50/51. Skip guards intact (test25/26/28/55).
- **test29 (RE-26) FAIL diagnosed & fixed — a test-setup artifact, NOT a
  regression.** After 130 `stage_lookup` calls, `governed_moments` count was 0.
  Root cause: **1.11.0 made the moment ledger LAZY / spool-based** (the
  `#146/#147` ask-signal removal + `#150/#151` record-instead-of-ask rework).
  Plain `stage_lookup` no longer folds into the `governed_moments` table eagerly
  — that table is minted only via `conformance_ask`. Instead the interceptor
  appends MomentSpool records to `<store>/moments.jsonl` (verified by probe:
  5 lookups+2 declares → 20 spool lines; 50 lookups → 158 lines).
  test29 was counting the obsolete eager-fold table → spurious `set
  up broken` FAIL with `bug_fixed=True`. Fixed the test to count the spool's
  `interception` lines as the busied-moment proxy → **back to correct XFAIL
  (exit 2)**: 133 interceptions for 130+1 lookups, no retention/prune surface,
  no auto-cap → RE-26 genuinely still present (S2, confirmed). Updated the
  test docstring to the 1.11.0 model.
- **DIRECTION #2 conformance ask-signal rework screened (small, correct):**
  - `#146/#147` "Remove the ask signal": the stage_lookup response no longer
    appends the "N earlier moments owe a question" block; `momentsWithRecordedAction`
    removed (engine.ts), ask-signal machinery torn out of mcp-server.ts.
    `momentId` + a rewritten `CONFORMANCE_INSTRUCTION`
    ("record whether the action followed these rules — conformance_answer ...")
    still ride on every rules-returning lookup.
  - `#150/#151` "Record instead of asking": `conformance_ask` stays (uninstructed;
    only writer of `asked_at`), `conformance_answer` now records the agent's own
    reading (first-write-wins, differing answer refused). NOT_FOLLOWED_INSTRUCTION
    swtiched to "Name which ... was not followed".
  - **No new MCP surface / tool count unchanged (21, conformance_ask/answer live).**
    test50 (round-trip) still PASSES green — the wire contract it pins is intact.
    No clean new RE gap: the rework is an internal instruction/ledger change, not
    an observability defect; `notAskedWithAction` is a read-side count surfaced on
    memory_overview (a behavior refinement, not a missing signal). RE-27
    (by-design: no rule-retirement surface) stands — no retirement/refuse tool
    added. No new RE registered (per DIRECTION triage-halt: existing-RE folding
    only; conformance rework folds clean into RE-27 by-design).
- No coverage run (DIRECTION #3 COMPLETE, raw-% 83.8% saturated; bump-only
  re-suite happened once as required).

## Metrics
- Tests 56, scenario coverage 100%, line coverage 83.8% (no coverage run),
  zero genuine FAIL.
- XFAIL 10, XPASS 8 — tally byte-identical to run-84/1.10.0, confirming 1.11.0
  resolves NO tracked RE and introduces NO regression.
- Matrix consistency check passes (confirmed→XFAIL, fixed→XPASS,
  source/by-design→`—`; RE-26 confirmed with test29 XFAIL holds).

## Next steps
- **1.11.0 release re-verify COMPLETE** — `.release_handled` already `1.11.0`
  (gate rolled it forward on installed==latest); PROD binary already upgraded by
  server-ops. No John action outstanding for this bump.
- **Worker**: sustain — next release bump is the only re-verify trigger. First
  flip candidates for the NEXT bump remain RE-17/test24 (storeInternal archived
  refuse) + RE-55/test56 (declare archived disclosure).
- **L2 queue unchanged**: RE-56(S3) + RE-53/46/54 structural set;
  RE-55(confirmed/XFAIL) + RE-26(confirmed/XFAIL) stay until disclose-or-refuse /
  retention ships.