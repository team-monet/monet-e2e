# 2026-08-27 — Run 73 (E2E worker, 1.9.0 release re-verify — DIRECTION #1)

Release gate: `NO_NEW_RELEASE 1.9.0`, `.release_handled=1.9.0` — the 08-27
supervisor pivot already marked 1.9.0 handled; prod binary is 1.9.0 (server-ops
upgraded). This run performs the actual worker re-verification per DIRECTION
priority #1: full-suite against the now-1.9.0 binary (isolated `~/.monet-test`
store, GR-01).

## New-install surface changes discovered (1.9.0 vs 1.7.1)
- `monet install` is **REMOVED** (`unknown command 'install' (Did you mean uninstall?)`).
- `monet uninstall` is the only hook-management command left (novel surface).
- `monet gate` is a **retired stub**: prints "Monet's gate is retired — this
  hook no longer evaluates anything and is failing OPEN (nothing is blocked).
  Run `monet uninstall`…" rc=0, and this notice is shown **ONCE** (stateful
  once-marker in the storage dir — a reused dir returns SILENCE on later calls).
- `retire-source` is a new command (not exercised this run).

## What was done (all in repo, English, PROD `~/.monet` untouched GR-01)
1. **RE-04 CONFIRMED FIXED (headline):** full-suite on 1.9.0 → test30 flips
   XFAIL→XPASS (exit 3). The CJK bigram lexical arm (#38/#97) now makes a Korean
   store produce 17 lexical tokens (was 0) and Korean semantic retrieval hits.
   RE-04 → `fixed`; test30 stays as the committed regression guard. This directly
   fixes John's real-world Korean-search gap.
2. **CLI-removal adaptation (install/gate retired), 4 tests:**
   - **test37** (was install-cli coverage) repurposed → `monet uninstall` surface:
     install-gone probe, no-hook rc0 no-op, scan-path isolation (both HOME wrapper
     and MONET_STORAGE_DIR wrapper listed → storage-path resolution is env-honoured),
     `--dry-run` byte-identical (read-only), real run removes the gate-hook handler,
     retired-gate notice pinned. 11 checks. **Flake found & fixed:** the gate
     notice-once marker persists on reused storage dirs → the final failed test had
     used shared `/tmp/x,/tmp/y`; switched to a fresh tempdir → deterministic 2/2.
   - **test39** (was gate-mirror evaluator) rewritten → retired-stub contract:
     `--help` documents retirement+uninstall, bare `monet gate` rc0 fail-open notice,
     `--mirror <nonexistent>` yields rc0 SILENCE (fail-open, never blocks), the old
     evaluator surface is gone. 8 checks.
   - **test55** (RE-52 install crash) → **SKIP**: `monet install` gone → the
     PostToolUse TypeError cannot manifest. RE-52 reclassified `confirmed`→`removed`
     (obsolete-by-removal, same precedent as the 1.7.0 source reclassifications).
   - **test50** (conformance roundtrip) adapted to **1.9.0 lazy moment
     materialization**: `governed_moments` is no longer folded eagerly at store-time
     (table absent until `conformance_ask` mints the row) and the store-time ASK-signal
     is gone. Pinned the lazy contract: no pre-ask row, judicable row materialized on
     ask (opened=1, rule_reads has the rule, outcome_at), re-ask preserves the recorded
     verdict. 17 checks. Old `answered_moment_no_longer_owed` store-signal assert was a
     GR-07 tautology → replaced with falsifiable verdict-persistence check.
3. Full-suite re-baseline (authoritative): **38/55 pass + 11 XFAIL + 6 XPASS + 0 FAIL**
   (exit 0) on 1.9.0. 13→11 XFAIL (RE-04→XPASS, RE-52→SKIP), 5→6 XPASS (RE-19/43/42/50/
   47 + RE-04). All 11 remaining XFAIL are the still-open behavioral bugs
   (RE-07/17/21/26/33/32/44/45/48/49/51). test09 clean (20/20 bursts both phases).
   test49 (structural band driver) survives intact — #95 tauConfident / centroid
   unification did NOT break it.
4. ISSUES.md: RE-04 → `fixed`, RE-52 → `removed`.

## Results
- 1.9.0 re-verify complete and green: 0 genuine FAIL across the whole surface.
- The highest-value tracked bug (RE-04, Korean search) is now FIXED and verified.
- Adapted tests convert the removed install/gate coverage to the new
  uninstall/retired-gate surface, so the suite tracks the 1.9.0 CLI reality.

## Next steps
- Worker: none additive (matrix green, queue empty, coverage COMPLETE). Keep the
  green suite; re-verify the 11 XFAIL at the next @team-monet/monet bump (esp.
  RE-17/test24 and RE-51/test54 which the retired-gate release left untouched).
- Supervisor: host-memory-pressure check on the standing `monet` MCP PID +
  test09 burst contention remains the one open operational item.

## Meta
- Worker: E2E/RE 1.9.0 release re-verify. Prod `~/.monet` untouched (GR-01/07).
  Docs/commits English. No coverage run (DIRECTION #3 COMPLETE; line % stays 83.8%).