# Local test data / runtime state (never commit)
monet.db
monet.db-shm
monet.db-wal
gate-mirror.json
inflight-*.json
slow-queries.jsonl
backups/
run-*/

# Schema-7 fixture + old-package prefix (regenerate via make_fixture_schema4.py)
fixtures/
prefix-old/

# Version-isolated test prefixes (npm install --prefix under MONET_TEST_DIR)
prefix-*/

# 1.7.0+ governed-moment spool written by the gate/conformance path to cwd
moments.jsonl
gate-journal.jsonl

# Local debug/probe scripts (not part of the public suite)
debug_*.py
dump_*.py
probe_*.py
scan_cron_concepts.py
verify_prod_search.py

# Python
__pycache__/
*.pyc
.venv/
build/
.cov-raw/
